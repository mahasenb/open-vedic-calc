# CLAUDE.md — open-vedic-calc

This repo is a generic, standalone Vedic (BPHS) astronomical calculation service, licensed AGPL-3.0-only. It is reached only over HTTP by a bearer-token-holding caller. Rules below supplement the workspace CLAUDE.md; they do not replace it. That parent file is inherited here automatically, but **the skills it names are not** — only `CLAUDE.md` traverses upward. When a skill the parent names is absent from your session's skill list, the parent states where to Read it; an unresolved skill is a file to open, never a rule to skip.

## Product neutrality — absolute

This is a public AGPL repo. It must **never** name, hint at, or link the proprietary consumer that calls it — not in code, comments, docstrings, tests, commit messages, PR titles/bodies, issues, or docs. Refer to callers only in generic, role-based terms: "the caller", "the HTTP client", "the consumer". "The downstream backend" is acceptable only as a role description, never paired with a name.

The CI gate `ci/check_no_proprietary_refs.py` (run by `.github/workflows/ci.yml` on every change, including docs) is the enforcement mechanism. `_FORBIDDEN` is a **list** of compiled patterns: a legacy base pattern (the standalone word this file's own guard describes, excluding the Swiss Ephemeris URL and the astronomy/astronomical domain words) plus zero or more additional brand tokens read at runtime from the `PROPRIETARY_REF_TOKENS` env var (comma-separated), which CI populates from a GitHub Actions secret of the same name (`.github/workflows/ci.yml`, `boundary` job `env:` block). Standing rule: keep the secret's token list current as the consumer's branding evolves — a token set that only guards a superseded name silently permits a leak; updating it when you learn the brand or infra identifiers have changed is required work, not optional (an **owner** action: only the repo secret's value changes, never this file).

Critical subtlety: do **not** write the literal brand token into this CLAUDE.md, into the gate file's plaintext, into its tests, or into any other tracked file — the file is public, and the token itself would leak the association. The forbidden-term list beyond the legacy base pattern must be maintained out-of-band, exclusively via the `PROPRIETARY_REF_TOKENS` secret injected at CI time — never as public plaintext. If asked to "add the brand to the gate," the correct implementation is adding/rotating that secret's value, not editing source. When the env var is unset (e.g. a fork or external PR without secret access), the gate falls back to legacy-only matching and does not fail — absent optional config is not itself a leak.

The gate scans the full pushed commit range, not just the latest commit: `ci.yml` passes `${{ github.event.before }}..${{ github.sha }}` as the gate's commit-range argument, so a leak buried in an intermediate commit of a multi-commit push cannot slip through. The gate falls back to scanning just HEAD's message when the range is absent or contains git's zero-SHA "first push" sentinel.

**The commit-message read is UTF-8 at BOTH ends of the pipe, and an unreadable message is a REFUSAL, never a pass.** `git log` is invoked with `-c i18n.logOutputEncoding=UTF-8`, so git transcodes each message into that encoding whatever the contributor's own git config says (measured: `i18n.logOutputEncoding=ISO-8859-1` in repository config makes `git log` emit latin-1 bytes, and a `-c` override beats it); the raw bytes are then decoded *here*, strictly, rather than inside `subprocess`. `text=True` decodes with the **locale** codepage — cp1252 on a Windows checkout — and does it on a reader thread, where a `UnicodeDecodeError` kills the thread and hands back `stdout=None`. Measured on the commit before the fix, that broke the two branches *differently and both wrongly*: the **range** branch (what CI passes) died with `AttributeError: 'NoneType' object has no attribute 'strip'` — a crash, not a verdict — while the **fallback** branch, which is exactly what the locally mandated `python ci/check_no_proprietary_refs.py` uses, turned that same `None` into an empty list, so the gate scanned **zero** commit messages and printed `OK: no proprietary consumer references found.` with a forbidden token sitting in the message it had just failed to read. A silently disarmed boundary gate is strictly worse than a crashing one. Strict-and-refuse rather than `errors="replace"` is a decision about *this* gate specifically, because it SCANS for tokens: replacement cannot hide an ASCII token (UTF-8 is self-synchronising, so ASCII bytes always decode to themselves, and U+FFFD is not a word character, so the legacy word-boundary pattern still matches beside one) but it does destroy a token spelled with a non-ASCII character — measured, such a token encoded as latin-1 decodes to a replacement character mid-word and stops matching — so an unreadable message is treated as possible concealment, not as absence. `CommitMessageDecodeError` is deliberately **not** an `OSError` subclass: `main` absorbs `OSError` so an absent git degrades quietly, and absorbing the refusal would restore precisely the silent pass it replaces. The sibling helper in `tests/test_line_endings.py` pins its encoding the same way but takes `errors="replace"`, and that asymmetry is deliberate and recorded in its docstring — it reaches its verdict from fields that are ASCII by git's construction, so no replacement character can change what it decides.

**The FILE scan is strict too, and for the same reason.** It used to read each file with `read_text(encoding="utf-8", errors="replace")`. That encoding was already pinned, so the file half was never locale-dependent — but *lossy is not the same as safe*, and the masking argument that decided the commit-message policy applies here unchanged: replacement cannot hide an ASCII token, but it destroys a token spelled with a non-ASCII character, which is exactly what a token arriving from a secret this repo cannot read may be. Both scanned inputs now decode through the one `_decode` helper, so the policy cannot be applied to one and forgotten on the other, and an undecodable file joins the same `refusals` list a bad commit message does — rc 1, naming the file, never `OK:`. Three properties are deliberate: the decode sits **outside** the `except OSError` arm (an unreadable file is skipped, an *undecodable* one is refused — absorbing the second into the first would restore the silent pass); a refusal **collects and continues** rather than raising, or one undecodable file would become a way to hide a leak in a readable one later in the walk; and `FileDecodeError`, like `CommitMessageDecodeError`, is **not** an `OSError` subclass. Measured before the switch: of the **104** files in the scan set, **0** failed a strict UTF-8 decode, so strictness costs nothing on a healthy tree and a failure is signal rather than routine noise. Contract: `ci/tests/test_check_no_proprietary_refs.py::TestFileScanDecoding`, which measures the masking itself before asserting it is shut — the premise is verified, not assumed.

**And the FILE scan's SCOPE comes from git, not from a directory walk.** It is `git ls-files -z --cached --others --exclude-standard` — everything that ships, plus everything one `git add` away from shipping (the gate's whole purpose is to run before the push that would publish the leak, and at that moment the offending file is usually still untracked), minus everything git is told to ignore. The walk it replaces was wrong in **both** directions at once. Fail **open**: `_SKIP_DIRS` drops a directory by NAME, so a tracked file under any of those names shipped unscanned — nothing tracked sits under one today, which is exactly why the hole was invisible, and measured, a tracked `data/notes.md` carrying a synthetic token was answered `OK: no proprietary consumer references found.` Scope **inflation**: measured 2026-08-26 in the shared root checkout, the walk enumerated **221** scannable files against a real set of **106** — **115** extra, **111** of them another branch's working tree under `.claude/worktrees/` — so the local gate's verdict depended on which unrelated branches happened to exist beside it. `-z` is not optional (git otherwise QUOTES a non-ASCII path and octal-escapes its bytes — the measured fail-open `ci/check_pytest_collection.py` shipped), and the listing is read as **bytes** and decoded in-process, so a filename that is not valid UTF-8 answers "git cannot answer" rather than dying on a reader thread. A walk is kept **only** as the fallback for a tree git cannot describe, and it announces itself; the new failure mode the switch introduces is closed in the same change — a scan that examined **zero** files is a REFUSAL, never a clean verdict, because `git ls-files` can answer rc 0 with no output where a walk of a populated tree cannot. Contract: `ci/tests/test_check_no_proprietary_refs.py::TestFileScanScopeComesFromGit`.

**And having got the right SET, the gate then opens ALL of it — there is no extension allowlist.** Asking git the right question fixed the scope; it did not fix what the gate did with the answer. `_is_scannable` admitted twelve extensions (`.py .md .yml .yaml .toml .txt .sh .ps1 .cfg .ini .json .dockerfile`) plus the exact basename `Dockerfile`, so a file whose extension nobody had thought of was enumerated, **skipped, and counted towards a clean verdict** — the gate printing `OK:` about a file it never opened, which is the directory blacklist's fail-open shape one layer along. Measured 2026-08-26, that left **EIGHT** tracked files outside the scan: `Dockerfile.test` (its `.test` suffix is not in the set and its basename is not exactly `Dockerfile`, so it matched neither arm — the gate read `Dockerfile` and skipped the file beside it), `.env.example`, `.github/CODEOWNERS`, `.gitattributes`, `.gitignore`, `.python-version`, `LICENSE` and `uv.lock`. The first three are prose a human writes about the deployment and about who owns which path — the register a downstream consumer gets named in. **Lock files are IN, deliberately**: `uv.lock` is tracked and a raw file view publishes it exactly like the code, it records dependency **names** and index URLs, and being machine-generated is the argument *for* scanning it rather than against — a private package name or index host lands there without a human ever typing it into a file the gate reads. **Binary is a REFUSAL, never a skip.** Widening means an unreadable file now reaches the strict decode, and the existing answer stands: it joins `refusals`, is named on stderr, and exits non-zero — "I could not read it" may never be recorded as "there was nothing in it", which would otherwise hand anyone a way to park a leak behind an unrecognised extension. Measured over the whole scan set at the time of the change: **0** files carry a NUL in their first 8 KiB and **0** fail a strict UTF-8 decode, so strictness costs nothing on a healthy tree; the local gate went from 106 to 114 files scanned. Contract: `ci/tests/test_check_no_proprietary_refs.py::TestEveryEnumeratedFileIsScanned`, whose last test asserts **enumerated == scanned** on the real tree, so reintroducing any filter — an extension set, a name test, a size cap — reds on the commit that adds it.

**A PR's TITLE and BODY are scanned too, by a separate checker — they are neither tracked files nor commit messages, so the tree gate never saw them.** They live only in GitHub's database, they can be edited after the PR is opened, and a PR page is world-readable and indexed exactly like the code. That made PR text the one artifact written most freely — by hand, while thinking hardest about the downstream consumer — with nothing checking it. `ci/check_pr_text.py` closes it, wired as the `pr-text` job in `ci.yml`. Four things about it are deliberate. (1) It **imports the pattern machinery from the tree gate** rather than reimplementing it, so the two can never disagree about what a forbidden token is. (2) The PR text reaches the process through `env:`, **never interpolated into a `run:` body** — a `${{ }}` expression is textual substitution performed *before* the shell parses the command, and anyone may open a pull request against a public repo, so a title carrying shell metacharacters would execute; `ci/tests/test_pr_text_check.py::test_pr_text_is_never_interpolated_into_a_run_body` pins that across *every* workflow, not just this one. **Both routes, not just the direct one:** `env:` is safe only while the `run:` body reads a SHELL variable (`"$PR_TITLE"`), and writing `${{ env.PR_TITLE }}` in the body throws that away — the same textual substitution, performed before the shell parses the command, on a value that was forwarded correctly. The dangerous env names are **derived** from each workflow's own `env:` blocks (any key whose value carries PR text, so renaming the variable is no escape) unioned with this repository's two conventional names as a floor — measured, derivation alone fails open when the assignment lives at another level and the step's own `env:` records nothing. Measured 2026-08-26: **no** workflow here takes the env route today, so that half is a preventive control, not a fix. **Both routes match on a NORMALISED expression body**, because GitHub accepts index notation and resolves property names case-insensitively: `${{ github['event']['pull_request']['title'] }}` and `${{ GITHUB.EVENT.PULL_REQUEST.TITLE }}` substitute exactly what the dotted spelling does, and measured, all three were unflagged before normalisation — the direct half of that gap predating the env route entirely. One bound is recorded rather than assumed away: only a *literal* index is resolved, since a computed one (`env[format('PR_{0}','TITLE')]`) would require evaluating GitHub's expression language, which is the modelling this guard deliberately does not do. The workflow scope itself comes from `git ls-files` and covers **both** `.yml` and `.yaml` — GitHub Actions runs `.yaml` identically, and a `*.yml` glob makes an affirmative "no violations" claim about a file it never opened — with `--others` keeping an untracked workflow in scope (the cheapest moment to catch a violation is the commit that introduces it, and the file is not yet tracked then) and `--exclude-standard` keeping a developer's ignored scratch workflow out. Outside a checkout it fails closed, because "zero workflows found" and "zero violations found" are the same green. **That enumeration is now the repository's ONE definition, `ci/workflow_scope.py`, and it had to be** — this guard got it right while two siblings that ask "does any workflow do X?" were still globbing `.github/workflows/*.yml`: `ci/tests/test_ci_runs_gate_regression_suite.py` and `ci/tests/test_swiss_ephemeris_job.py`. The second is the one that mattered, because every assertion in it has the form *no* workflow narrows the accuracy job, injects `PYTEST_ADDOPTS`, writes `$GITHUB_ENV` or unpins uv — all of which a `.yaml` workflow could have done while the guard reported clean by not looking. `ci/tests/test_environment_convergence.py` already read both suffixes and now delegates too, so there is one answer rather than four; the fail-closed refusal is a dedicated `WorkflowScopeError` rather than a bare `assert`, which `python -O` strips. (3) `on: pull_request` gains an explicit `types:` list including **`edited`**, which the default set (`opened`, `synchronize`, `reopened`) omits — without it a PR could be opened clean and the leak edited in afterwards while the check still showed the green it earned on the original text. (4) The **secret-availability decision is fail-closed with an enumerated exception**: on a same-repo PR the secret is supplied, so an empty `PROPRIETARY_REF_TOKENS` is a **hard failure** (exit 2) rather than a quiet fallback to legacy-only matching — a control must never run silently at reduced strength — while the two cases where GitHub *withholds* it are advisory. Those cases are a **fork PR** and a **Dependabot PR**; the second is the one a naive "same repo means secrets" rule gets wrong, because a Dependabot branch lives in this repository (so `head.repo == repository` holds for it) while the run is served from the separate Dependabot secret store, and hard-failing on that would redden every weekly bump. Advisory scopes the **missing secret, never the verdict**: the legacy pattern needs no secret and stays live, so a match still fails the job on a fork. The checker carries `--self-test`, run as its own step *before* the scan, so a detector that has stopped matching stops the job instead of certifying the PR text green. Contract: `ci/tests/test_pr_text_check.py`; extend it, never weaken it.

Before every push, run the gate locally: `python ci/check_no_proprietary_refs.py`. Its behaviour (list-of-patterns, env-var token injection, commit-range scanning, fail-safe fallbacks) is covered by `ci/tests/test_check_no_proprietary_refs.py`. That file lives outside the `tests/` tree by design and is **not** swept by `pytest tests/`; `test.yml` and `publish.yml` each run it as its own explicit CI step (`pytest ci/tests/ -q`), separate from the main suite, so a future edit that regresses `_FORBIDDEN` back to a single regex or drops the env-token loop cannot pass CI green. `ci/tests/test_ci_runs_gate_regression_suite.py` is the regression guard on that wiring itself — it asserts some workflow step actually invokes `ci/tests/`. Extend both test files, never weaken them, and never put a real or synthetic-looking-real brand literal in the gate's test file either (it uses only an obviously-fake synthetic token, injected via the same env var).

## Engine conventions (astronomical correctness)

- Ayanamsa is LAHIRI. The ephemeris path and sidereal/ayanamsa mode are **THREAD-local** state (pyswisseph >= 2.10's thread-local `swed` struct), **not process-global** — every thread must apply both itself before it calls into `swe.*`/`drik.*`, or it silently computes on the Moshier fallback with the default Fagan/Bradley ayanamsa (~0.88 deg / 53 arcmin off Lahiri) instead of raising. `bphs_core/utils.py`'s `_ensure_thread_ephemeris_state()` applies `swe.set_ephe_path(...)` and `drik.set_ayanamsa_mode('LAHIRI')` once per thread, idempotently; `serialized_ephemeris` calls it on every invocation of every C-library entry point, so a new compute path gets it by construction as long as it carries that decorator — never assume a clean default on a thread that isn't provably decorated. `Chart._compute` additionally re-asserts `drik.set_ayanamsa_mode('LAHIRI')` inline (the first statement of `Chart._compute`, `bphs_core/chart.py`) on top of the decorator's guarantee; keep that re-assertion pattern in any new compute path as defense-in-depth — but the decorator, not the inline re-assertion, is what makes a new thread correct.
- **Ephemeris lock (thread-safety):** the underlying Swiss Ephemeris C library is not documented as safe for concurrent `swe_calc`/`swe_houses` calls from multiple threads — even once each thread's own path/ayanamsa state (above) is correctly configured — and Starlette's request pool + the scan-job pool in `app/jobs.py` both enter it concurrently. Every **new** runtime compute path that calls `swe.*`/`drik.*`/`charts.*` **must** be wrapped in `@utils.serialized_ephemeris` (bphs_core/utils.py) so it (a) gets the per-thread state above applied on every call, and (b) holds the process-wide `EPHEMERIS_LOCK` — and it must be decorated at **inner, millisecond-bounded granularity** (one chart, one day, one instant, one JD conversion), **never around a whole scan**: a whole-scan hold blocks every interactive route and the async submit path for the scan's remaining duration, reversing the "submissions never wait on scan compute" property below. The lock is an RLock, so nested decorated entry points are safe. Add each new entry point to the coverage list in `tests/test_ephemeris_lock.py::test_every_c_library_entry_point_is_serialized` — the test pins only the known set; this rule is the guard for future paths.
- Julian Day discipline (the rule that prevents the tz double-subtraction bug): pyjhora chart/drik functions (`charts.rasi_chart`, `drik.ascendant`, `drik.dhasavarga`, `drik.planets_in_retrograde`) expect a **local-clock JD** and subtract the place timezone themselves internally. Build the local JD and pass it **unmodified** to pyjhora — never pre-subtract the timezone (`_jd_from_person`, `bphs_core/chart.py`). Any swisseph call that needs true UT derives it separately as `jd_utc = jd - tz_offset_hours / 24.0` **at the call site** (`Chart._compute`, on the line after its `_jd_from_person` call). Never feed a tz-pre-subtracted JD to pyjhora, and never feed a local JD to a swisseph UT function — the two transports are distinct and are not interchangeable.
- The lagna/ascendant is computed **directly** from swisseph — `swe.houses(jd_utc, lat, lon, b"P")` → `ascmc[0]`, then sidereal-corrected as `(ascmc[0] - ayanamsa) % 360` (the `swe.houses` call in `Chart._compute` and the `sid_asc` assignment that follows it). **Never** take the ascendant from pyjhora's `rasi_chart()[0]`: it double-applies the timezone and lands the lagna tz-hours off. Fallback: if placidus (`b"P"`) raises (extreme latitudes), fall back to equatorial (`b"E"`) and record `house_system` accordingly (the `except` arm of that same `swe.houses` call, which sets `_house_system_used = "equatorial"`) — never silently return a placidus-labelled chart from an equatorial computation.
- Whole-sign is the primary house frame for planetary house assignment; Bhava-Chalit cusps are provided separately as sidereal Placidus cusps (the `chalit_cusps` assignment in `Chart._compute`, served as `ChartSnapshot.chalit_cusps`). Do not conflate the two — whole-sign house is the interpretive default, chalit is supplementary.
- **Served date range: MEASURED, never taken from the data files' label — `MIN_EPHEMERIS_DATE = 1800-01-02`, `MAX_EPHEMERIS_DATE = 2400-01-09` (`app/schemas.py`). This is the POINT-LOOKUP span; the scanned-day fields are bound more tightly (`MIN_SCANNED_DATE`/`MAX_SCANNED_DATE`, two bullets down) — never quote this pair as the range for *every* date field.** Upstream labels `sepl_18`/`semo_18`/`seas_18` "AD 1800–2400", and the bound used to be a YEAR range that took that literally, advertising through 2400-12-31. The files stop earlier: measured with `probe_ephemeris_source()` at one-day steps, the last day the seven visible grahas answer from the data is **2400-01-10**, and from 2400-01-11 every one returns `FLG_MOSEPH`. **That does not error — it answers.** Measured end-to-end on `/v1/chart`, spying on every `swe.calc_ut` a served request makes: 2400-01-10 → 200 with 15/15 calls on Swiss data; 2400-01-11 → 200 with **12/15 on Moshier**; 2400-06-01 and 2400-12-31 → 200 with **15/15 on Moshier**. So 355 days of advertised range were being served off the fallback engine at HTTP 200 with nothing in the response saying so — the same class of defect as CALC-1's `/healthz`, one layer out. The **one-day margin at each end is load-bearing, not padding**: these bound a local DATE while the ephemeris is queried at a UTC INSTANT, and `timezone_offset_hours` spans [-12, +14], so one local date covers UTC instants from (D-1 10:00) to (D+1 11:59:59). Measured, a bound sitting exactly on the file boundary still leaks — 2400-01-10 23:59:59 at tz=-12 was 12/15 Moshier, and the old lower bound leaked identically (1800-01-01 00:00:00 at tz=+14 was 12/15 Moshier). `tests/test_birth_date_ephemeris_range.py` re-measures both halves on every Swiss run: that every admissible `(birth_time, timezone_offset_hours)` corner inside the bound is Swiss-backed, **and** that the data really stops just past it, so narrowing the range to something arbitrarily small does not pass either. Never widen this to the label without re-measuring against the files actually shipped.
- **`birth_date` was never the only date on the wire, and all TEN of the others are bounded — by TWO spans, not one.** `at_date` (`/v1/transits`), `from_date`/`to_date` (`/v1/dashas`), the three `start_date`/`end_date` pairs (`/v1/muhurat`, `/v1/muhurat/lagna-shuddhi`, `/v1/muhurat/family-lagna-shuddhi`, and the two `/async` submit forms that share those models), and `reference_date` (`/v1/compat`) are all bounded through `_bounded_iso_date_str`/`_bounded_date`, with the 422 message and the OpenAPI description both generated from the constants. **There are TWO spans, not one, and which one binds a field is decided by whether the field is a POINT lookup or a SCANNED DAY** — see the scanned-day bullet below. The four point-lookup fields (`at_date`, `from_date`, `to_date`, `reference_date`) take `MIN_EPHEMERIS_DATE`/`MAX_EPHEMERIS_DATE`; the six scanned-day fields (the three `start_date`/`end_date` pairs) take the narrower `MIN_SCANNED_DATE`/`MAX_SCANNED_DATE`. A test that asserts one span for all ten fields passes only by accident — assert the span that binds *that* field. Measured on the commit before the bound landed: `at_date=9999-01-01` and `reference_date=9999-01-01` were **uncaught 500s** (`swisseph.Error` from `drik.sidereal_longitude`, and `OverflowError: date value out of range` from `compute_dasha_overlaps` in `bphs_core/compat.py`, where its `window_start + timedelta(days=25 * 365.25)` runs past year 9999); `at_date=2400-06-01` was 200 with 10/25 calls on Moshier, `2500-01-01` 200 with 71/86; and a muhurat/lagna-shuddhi/family scan at 2400-06-01 was 200 with 1450/6353, 1650/6553 and 3300/13106 calls on Moshier respectively. **The tenth field is the one a sweep misses:** `reference_date` is a `date`, not an `IsoDateStr`, so it does not match the grep that enumerates the other nine — enumerate by TYPE and by endpoint, not by one alias. **And COUNT what the grep returns instead of eyeballing it:** the first pass here read the three `start_date`/`end_date` pairs as two, reported eight `IsoDateStr` sites where the base file has nine, and so described a change that correctly bound TEN fields as binding nine — in five places, through a review round. The code was right the whole time; only the prose was wrong, which is precisely the failure a hand-typed count produces. `tests/test_date_field_ephemeris_range.py` now DERIVES the enumeration from the schema module and asserts both the count and that the case table covers every field, so the number cannot drift from the code again.
- **A "does this field have a validator?" check is not a "is this field bounded?" check.** Measured while building the guard above: `IsoDateStr` carries its own `AfterValidator` (the ISO *shape* check), so a checker that tests for the PRESENCE of an `AfterValidator` calls every bare `IsoDateStr` field bounded — reverting `at_date: BoundedAtDate` to `at_date: IsoDateStr` left that first version GREEN, the exact regression it existed to catch. The guard now CALLS each validator with an out-of-span probe and requires it to raise: a shape validator accepts `2500-01-01`, only a range validator refuses. Separately, the two halves of the annotation must BOTH be read — for a plain `x: BoundedAtDate` Pydantic hoists the metadata onto `FieldInfo.metadata` and leaves `annotation` bare, while for `x: BoundedReferenceDate | None` it does the opposite and leaves `metadata` EMPTY with the validator inside the union member. Measured: a checker reading one place is wrong about half these fields, and it reported `reference_date` unbounded while the validator was demonstrably refusing `9999-01-01`. Both arms are neuter-verified.
- **Two things this bound deliberately does NOT claim.** (1) `from_date`/`to_date` and `reference_date` drive **zero** ephemeris lookups, because dasha periods are projected arithmetically off the natal Moon. **Demonstrate that on a pair the service will actually SERVE** — the obvious probe does not: birth 1950-06-15 with `to_date=2999-12-31` is refused 422 by the birth-relative `MAX_DASHA_DAYS` cap having made **zero** calls, so it shows nothing either way, and an earlier draft of this entry cited it as "15 calls at 2999-12-31", a number no request can produce. A late birth keeps the cap satisfied: measured at base, birth 2390-06-15 with `from_date=2395-01-01` and `to_date` at 2400-01-09, 2450-01-01 **and** 2500-01-01 served 200 every time with the `swe.calc_ut` count pinned at exactly **15** (the natal chart), the last running ~100 years past the span. `/v1/compat` needs no such care, having no cap: **30** calls (its two natal charts) at `reference_date` 2026-05-26, 2400-01-09, 2500-01-01 and 3000-01-01 alike. They are bounded for the crash and for one consistent served span, **not** because a fallback answer was measured behind them; a "Swiss-backed" spy assertion on those fields would pass by construction and measure nothing, so none is written. The deliberate cost: a late-born chart can no longer request a timeline past `MAX_EPHEMERIS_DATE` (measured, birth 2390-06-15 with `to_date=2500-01-01` used to be a 200). (2) **The bound does not make every muhurat scan Swiss-backed at the edges of the span, and no test says it does — but get the CAUSE and the COUNTED QUANTITY right, because the first version of this entry had both wrong.** It blamed `bphs_core/muhurat.py::_is_eclipse_day` on the strength of a `swe.calc_ut` spy. That instrument cannot see the eclipse finders at all: `swe.sol_eclipse_when_loc`/`lun_eclipse_when` compute their positions inside the C library and never re-enter the Python binding, so the spy records **zero** calls for the entire limb (measured at 2400-01-09, 1800-01-02 and 2026-05-26 alike; pinned by `tests/test_muhurat_edge_probe_engine.py::test_a_calc_ut_spy_is_blind_to_the_eclipse_finders`). Re-measured by call site, the out-of-day reaches that spy DID see belong to `_is_adhik_maasa` (`drik.lunar_month`) — 1500/1864 Moshier calls at 1800-01-02, every one of them reaching back to 1798-07-10, and 48 reaching forward to 2400-01-29 at 2400-01-09 — plus the day's own panchanga limbs (`drik.tithi` first, inside `compute_muhurat_for_day`), 294 of the 342 at 2400-01-09. **Name that call site by function, never by line number** — the first draft of this entry cited the tithi call by line number: correct at the base commit, and wrong by the time the sentence was written, because the same change added ~25 lines to `_is_eclipse_day` above it and shifted that call (492 → 517). That is this file's own "a citation that rots silently is worse than none" rule failing inside a single commit, and it is not hypothetical: a sweep of the tree's other `file:line` citations found further pre-existing ones that no longer point at what they claim. **How many is deliberately not written here.** A count in this file is itself a hand-typed number that nothing recounts — it would rot on the next edit to any cited file, which is the very defect this paragraph is about. Two review rounds on the change that added this text died on exactly that, and the sweeping instrument has known blind spots besides — a citation naming two lines of one file at once, prose phrasings, and citations into vendored dependencies whose line numbers move with the pinned version — so any total would be one instrument's lower bound presented as a fact. The enumeration is reported upward against a stated commit, where it is a dated measurement rather than a standing claim; that sweep has since landed, and the anchor convention it established is now **enforced on every run** rather than re-measured by hand — see the citation-anchor bullet under **Tests & local gate**. **Raw "Moshier calls" is not accuracy lost:** a scan at 1800-01-02 makes 1500 of them and loses nothing, because every one is at a Julian Day with no data to read. The number that means lost accuracy is a Moshier answer at a JD the files DO cover, which happens because swisseph **keeps** the fallback after an out-of-range read — measured in a fresh process: probe Moon 2400-01-09 → SWISS (65602); read Moon 2400-01-29 → MOSHIER (65604); probe Moon 2400-01-09 **again** → MOSHIER (65604). `utils.restore_ephemeris_state()` clears that, and `_is_eclipse_day` now calls it in a `finally`. Measured after the fix: the **low edge loses zero** in-span accuracy on all 58 dates sampled 1800-01-02…1801-07-14; the **interior is clean** including 10- and 30-day RANGE scans at every timezone corner (a single-day pin was too weak — the leak can cross days inside one scan, and the local-day boundary moves with the timezone); the **high-edge residual was real and timezone-dependent**, earliest scanned day 2399-12-24 (tz −12, 4 calls) and dense over the final week (up to 303 at 2400-01-09). **That residual is now CLOSED, by a separate and narrower SCANNED-DAY bound — see the next bullet** — and `tests/test_date_field_ephemeris_range.py::test_no_accepted_scanned_day_loses_accuracy_to_the_fallback` (which replaced `::test_the_edge_residual_stays_at_the_edge`) now asserts the last accepted days ARE clean, which its predecessor deliberately refused to claim because they were not. The eclipse **verdict** was never at risk and is pinned separately: over 44 paired eclipse events the two engines drift at most **2.714 s** against a closest-approach-to-local-midnight of **185 s** (a 68x margin), with **0** disagreements over 132 days tested.
- **A SCANNED DAY is not a point lookup, and it carries its own narrower bound — `MIN_SCANNED_DATE = 1800-01-02`, `MAX_SCANNED_DATE = 2399-12-01` (`app/schemas.py`).** `birth_date`/`at_date` read the instant they name, so a one-day margin for the timezone span is enough. `compute_muhurat_for_day` does not: it asks questions whose answers are *by construction* not on the scanned day. Measured per limb with a **signature-agnostic** `swe.calc_ut` spy over 702 probes (117 scanned days across three synodic months plus a year spread × 6 tz corners), as days from the scanned date's 00:00 UT — `drik.lunar_month` (via `_is_adhik_maasa`) **+31.834 d** / −2025.746 d, `drik.yogam` **+24.859 d**, `drik.nakshatra`/`varjyam`/`amrita_gadiya`/`panchaka_rahitha` +2.543 d, `drik.tithi`/`karana` **+1.998 d**. **Two corrections to what this file used to say:** `drik.tithi` was named as a principal cause of the end-of-range residual and is in fact the *smallest* of these reaches, while `drik.yogam` — which nothing here mentioned — is the second largest. And the earlier instrument was declared `def spy(jd, planet, flags, *a, **k)`, which survives pyjhora's `swe.calc_ut(jd, planet, flags=flags)` **only because its third parameter happens to be spelled `flags`**; rename it and the probe raises `TypeError` and silently records nothing — a measured instance of "a zero must mean absence, not a blind probe". The forward reach is a property of the lunar month, not the era: re-measured driving `compute_muhurat_for_day` whole across 8 tz corners it is **+31.835 d** over 1800-01-02..1806-07-30 and **+31.815 d** over 2398-12-01..2399-12-01, against a mechanism ceiling near 31.4 d (one synodic month ≤29.83 d + the day + the 0.583 d tz span). **Derivation:** the shipped Sun/Moon data was *bisected* against the files actually present and answers JD 2378496.4998 (1799-12-31 23:59 UT) → JD 2597651.3531 (2400-01-10 20:28 UT); a scanned day of 2399-12-01 sits at JD 2597610.5, leaving a **40.853 d** budget against the 31.835 d worst reach — a 9.018 d margin. The *tight* limit is 2399-12-10, which clears the measured reach by **55 minutes**; that is a coincidence, not a bound, which is why the served bound is not set there. **Cost, stated rather than discovered later:** 39 days at the top of the range — a scan may no longer be requested for 2399-12-02..2400-01-09, which it previously answered at HTTP 200 with up to 303 of ~2800 calls taken from Moshier at Julian Days the files DO cover. **The LOWER bound is deliberately NOT narrowed**, and that is a measurement, not an omission: `drik.lunar_month` also searches backward to an *anchor* (from 1800-01-02 it reads −540.974 d, landing on 1798-07-10, and the distance grows while the anchor stays put, then resets — a sawtooth to −1605.399 d at 1805-10-03), but measured, the fallback stickiness is **not symmetric** — an in-span lookup after that backward search still answers from Swiss data, so bounding it away would cost ~4.5 years of served range and buy nothing. `tests/test_muhurat_edge_probe_engine.py::test_adhik_maasa_probe_leaves_the_scanned_day_on_swiss_data` pins that asymmetry precisely because it is the evidence for a decision *not* to change something. Contract: `tests/test_scanned_day_ephemeris_reach.py` (cause) + `tests/test_date_field_ephemeris_range.py::test_no_accepted_scanned_day_loses_accuracy_to_the_fallback` (consequence); extend them, never weaken them.
- **A date bound can shadow a range CAP and silently retire it.** `MAX_DASHA_DAYS`/`MAX_MUHURAT_DAYS`/`MAX_LAGNA_SHUDDHI_DAYS` are enforced in the endpoint handler, which runs *after* Pydantic — so once the span bound landed, the far-future probes that used to exercise those caps (`to_date=9999-12-31`, `from_date=9900-01-01`) were refused by the schema first and the cap tests would have passed with the cap **deleted**. Their probes were re-pointed at in-span dates where the cap is unambiguously the binding constraint (`to_date=MAX_EPHEMERIS_DATE` against a 1950 birth is ~450 years, far past the 128-year cap). When adding a boundary check upstream of an existing guard, re-verify that the existing guard's tests still reach it.
- **Lunar node model: the TRUE (osculating) node — owner-ruled 2026-08-17 — DECLARED in `bphs_core/utils.py` (`LUNAR_NODE_MODEL`) and never inherited.** Rahu is the true node; Ketu is exactly Rahu + 180°. This is a decision, not a default that happened to be in force. The engine already served the true node when the owner ruled it, so the ruling records authority and changes no served longitude. Measured over 1800-01-01..2400-12-31 at one-day steps against the real Swiss data files, the two models differ by up to **1.981465100° = 7133.2744 arcsec** (worst instant 1905-10-10 22:00 UT, refined at one-hour steps), and across those 219511 sampled days they place Rahu in a **different nakshatra pada on 28.85%** of them, a different nakshatra on 7.19% and a different rasi on 3.20%. That is more than twice the ~0.88° ayanamsa error above — an undeclared model was a ~2° accuracy exposure sitting behind a dependency resolve. Rationale for TRUE: every other graha here is a true/apparent modern-ephemeris position, so serving a smoothed mean element for one graha alone would make a single chart internally inconsistent. Second-order consequence worth knowing: the mean node is analytical and ephemeris-source-independent (measured max Swiss-vs-Moshier **0.000000295°**) while the true node carries the full ephemeris-source error (measured max **0.018175452° = 65.4316 arcsec**, at 1936-10-30), so this choice also decides whether an ephemeris-source defect can touch Rahu/Ketu at all.
- **The node model has three independent levers, and the obvious one is not the one that serves.** Measured, not read from documentation: set `jhora.const._RAHU` / `_use_true_nodes_for_rahu_ketu` to the mean node, compute a `Chart`, and it returns the true node anyway — unchanged to the last digit. The chart path is `charts.rasi_chart` → `drik.dhasavarga(jd, place, 1)`. Under pyjhora 4.8.6 `dhasavarga` declared `set_rahu_ketu_as_true_nodes=True` as a **literal** parameter default (not `None`), so it rebuilt the body table from that literal and never consulted the constants. `utils.apply_lunar_node_model()` therefore pins (1) the constants through `const.set_node_mode` — never by assigning `_RAHU` directly, which would desynchronise it from the boolean that `set_planet_list` reads — and (2) the mutable `drik.planet_list` global, and **verifies** (3) `dhasavarga`'s serving default, which this repo cannot set. Any lever disagreeing with the declaration is a **hard failure at import**, never a warning; `bphs_core/__init__.py` imports `utils` first so the pin lands before any sibling module uses drik. **pyjhora 4.8.7 changed lever 3**: the default is now `None` and the forcing body is commented out upstream ("it is altering/forcing other than configured values"), so the serving path now *defers* to the constants lever 1 pins. `_serving_path_node_default()` was already written for that case — it accepts `None` precisely because it means "defers to the constants, already pinned" — and the served node did not move (measured on 4.8.7: Rahu at 2000-01-01 12:00 UT = 100.100800°, equal to swisseph's TRUE node, 3911.8″ from MEAN). One consequence is recorded rather than left to be discovered: under 4.8.6, declaring `LUNAR_NODE_MODEL = "mean"` hard-failed at import because the literal would have kept serving true nodes under a mean banner; under 4.8.7 it no longer hard-fails — measured, it serves 101.187424°, the actual mean node. That is a guard whose *reason expired* rather than a guard lost: the banner-vs-reality contradiction it detected cannot arise once the serving path honours the declaration. The friction that stops a one-word model switch is unchanged — `tests/test_lunar_node_model.py::test_the_declared_lunar_node_model_is_the_true_node` pins the literal, and the Swiss goldens move ~1° on both shadow grahas. `pyjhora` is pinned to an exact version (`==4.8.7` in `pyproject.toml`, matching `uv.lock`) for the same reason `pyswisseph` is — every one of those three levers lives inside it. Contract: `tests/test_lunar_node_model.py`; extend it, never weaken it.
- **Shadow-graha retrogression: DECLARED in `bphs_core/utils.py` (`PERPETUALLY_RETROGRADE_GRAHAS = {"Rahu", "Ketu"}`) and never inherited.** BPHS reads both lunar nodes as permanently retrograde (vakri), and this engine serves them that way as a stated decision. What it served before the declaration existed was the **exact inverse**, on every chart: retrogression reaches a chart through `drik.planets_in_retrograde`, and under pyjhora 4.8.6 that function dropped both nodes from its loop unconditionally (`_planet_list = {p: p_id for p, p_id in planet_list.items() if p not in [const._RAHU, const._KETU]}`), so it could never name them and `chart.py`'s `pid in retro_planets` was `False` for Rahu and Ketu on every date. **pyjhora 4.8.7 reversed that**: the filter now reads `if p not in [const._SUN, const._MOON]` and the library reports the nodes' *computed* direction — measured over 400 instants from 1990-01-01 at 37-day steps, it calls Rahu retrograde on 72.25% and **direct on 27.75%**, while this engine still serves retrograde on **100%** of charts because `graha_is_retrograde` ignores `computed` for these two names. Nothing served moved on that bump; had the flag been inherited it would have flipped on roughly a quarter of all charts with nothing in the diff to say so. The override is therefore **more** load-bearing than when it was written — it used to turn an absence into `True`, and now overrules a live, contrary answer. Measured 2026-08-07 on the frozen resolve over 1461 charts (2000-01-01..2019-12-27, 5-day steps): both shadow grahas retrograde on **0**, while the same sweep saw Saturn retrograde on 35.93% and Mercury on 19.16% — the sweep could observe retrogression and simply never saw it here. **Structural, not a sampling or ephemeris-runtime artefact**: identical under the real Swiss data files and the Moshier fallback. Why declared rather than derived from the ephemeris — under the declared TRUE (osculating) node the speed sign is *not* constant: over 350400 hourly Swiss samples (1990-01-01..+40y) the true node's longitude speed is **positive (direct) on 25.82%** of them, peaking **+0.128202129°/day at 2029-06-21 00:00 UT**, while the mean node is retrograde on 100% of the same span — so deriving the flag would report Rahu direct on roughly a quarter of charts, and the classical reading is a deliberate override of that signal, not a restatement of it. Near the turning points that sign is also ephemeris-source-dependent (**the same 2029-06-21 instant reads −0.001408427°/day under Moshier** — opposite sign), so a derived flag would not even be stable across the two runtimes this repo supports; keep any assertion on it out of `tests/` and inside the Swiss-only job. Applied by graha **name** through `utils.graha_is_retrograde(name, computed=…)` at **both** `chart.py` construction sites (the rasi loop and `_build_varga_chart`) — pinning one reads exactly like pinning both — and a declared name absent from `utils.PLANETS` **hard-fails at import** rather than silently matching nothing and handing the nodes back to the library. Blast radius, measured across four charts (including ones with retrograde classical grahas): **22 leaves move and all 22 are `is_retrograde` on Rahu/Ketu across the rasi chart and the ten vargas — Shadbala, Bhavabala, Ashtakavarga, Vimshopaka and combustion are byte-identical.** `_cheshta_bala` is the one strength limb that reads the flag, and it is only ever reached for the seven grahas in `SHADBALA_MINIMUMS` plus house lords from `get_sign_lord`, none of which is a node; `_apply_combustion` skips any graha absent from `_COMBUSTION_ORB`, which excludes both. Contract: `tests/test_shadow_graha_retrogression.py`; extend it, never weaken it.
- **Classical-graha retrogression in the divisional charts: PROPAGATED from one computation, and now observed.** Retrogression is a property of the INSTANT, not of a divisional mapping — a varga re-maps longitude onto a finer grid and cannot change the direction a graha is moving — so the flag every varga serves must equal the rasi flag for every graha. Structurally it already does: `_compute` calls `drik.planets_in_retrograde` **once** and hands the same result to all ten `_build_varga_chart` calls, so there is exactly one link where it can be severed, and measured across eight charts × eleven charts × seven classical grahas there were zero varga/rasi disagreements. What was missing was anything that would notice. Measured 2026-08-09 on the frozen resolve against the real Swiss data files: replacing `_build_varga_chart`'s `computed=pid in retro_planets` with `computed=False` — reporting every classical graha DIRECT in all ten vargas on every date — left the entire suite green at **741 passed, 0 failed**, and so did the inverse (`computed=True`, blanket vakri), and so did severing D9 alone, and so did dropping the flag in `app/main.py`'s `_pd_to_schema`. The shadow grahas were pinned in every varga, but `graha_is_retrograde` returns True for Rahu and Ketu whatever `computed` says, so those assertions are blind to precisely this mutation. Contract: `tests/test_varga_retrogression.py` — three synthetic instants chosen for **margin from station** (worst 38.08 d / 11.83 d / 50.33 d, hourly resolution; the slowest graha at any fixture sits 3.2e5 × its own Swiss-vs-Moshier speed disagreement away from zero), each of the five tara grahas observed **both** retrograde and direct so a table that decayed into all-direct fails its own guard, the served set asserted per varga as an **equality** (a blanket `True` fails as loudly as a blanket `False`), and the fixture table itself cross-checked against an independent central-difference of `utils.graha_sidereal_longitude` so the goldens are verified rather than recorded. Verified identical under **both** ephemeris runtimes. The two lunar nodes are deliberately **not** re-derived there — that file builds its expectations from `PERPETUALLY_RETROGRADE_GRAHAS`, and `tests/test_shadow_graha_retrogression.py` remains the tripwire on the declaration itself (measured: shrinking the declaration reddens that file and leaves this one green, which is the intended division). Extend it, never weaken it.
- **Position-flag word: DECLARED in `bphs_core/utils.py` (`POSITION_FLAGS = 65810`) and never inherited.** The third argument to `swe.calc_ut` decides the reference frame every served longitude is expressed in, and it used to come from `drik.PLANET_FLAGS`, which pyjhora builds at *its* import time from five `jhora.const.PLANET_POSITIONS_*` globals. Measured 2026-07-31, this repository unchanged, on two resolves: **pyjhora 4.8.6 → 66386, pyjhora 4.8.7 → 65810**, a difference of **576 = `FLG_NOGDEFL` (512) | `FLG_NONUT` (64)** — 4.8.7 flipped `PLANET_POSITIONS_USE_DEFLECTION` and `PLANET_POSITIONS_USE_NUTATION` from `False` to `True`. **That bump was taken deliberately on 2026-08-21 and the declaration re-derived with it**; the pin is now `pyjhora==4.8.7` and the declared word is `FLG_SWIEPH|FLG_SIDEREAL|FLG_TRUEPOS|FLG_SPEED`. `FLG_TOPOCTR` is deliberately not taken (this service receives no observer altitude), and `FLG_NOGDEFL`/`FLG_NONUT` are no longer taken either — measured, swisseph applies both unasked and reports so in its retflag (requesting 65810 returns **67410**, carrying `FLG_NONUT|FLG_NOGDEFL|FLG_NOABERR` nobody asked for), so the two words are the *same computation*, not merely close: longitude **and** daily speed are **bit-identical** across 72 graha-epoch pairs spanning 1800–2400, 56 answered on Swiss data and 16 on Moshier. Because the pair is now relied on rather than declared, the reliance is **enforced**: `utils._IMPLIED_BY` maps each omitted flag to the declared flag that masks it (`FLG_NOGDEFL`←`FLG_TRUEPOS`, `FLG_NONUT`←`FLG_SIDEREAL` — measured separately; clearing `FLG_TRUEPOS` stops swisseph adding `NOGDEFL` back, while `NONUT` is still added for any sidereal request), and `apply_position_flags` step (0b) hard-fails if a masking flag ever leaves the word while the flag it masks is absent. **The flags are coupled and only one of them currently moves a number.** Measured with the frozen resolve against the real Swiss data files over 1800-01-01..2400-12-31 at one-day steps, 219511 days × 9 grahas: dropping `FLG_TRUEPOS` (geometric → apparent) moves positions by up to **0.016817256° = 60.5421 arcsec** (Mercury, 2353-06-16 00:00 UT; Venus 44.7711″, Mars 38.9693″, Jupiter 30.0595″, Saturn 27.2519″, Sun 20.8489″, Moon 0.7632″, Rahu/Ketu 0.0146″), while dropping `FLG_NOGDEFL`+`FLG_NONUT` moves **0.000000000° on every graha on all 219511 days** — because under `FLG_TRUEPOS` swisseph ignores deflection and aberration outright, and for a sidereal request it applies `FLG_NONUT` unasked (retflag 65602 for a plain `FLG_SWIEPH|FLG_SIDEREAL` request). So the library's word moved and **no golden here could have caught it**: the served numbers were bit-identical, and when the bump was actually taken the tripwire was the *only* thing that went red — measured, `pytest tests/` produced **41 collection errors**, every one the applier's `RuntimeError`, while `pytest ci/tests/` stayed green at 155 passed. That inertness is conditional — clear `FLG_TRUEPOS` and `FLG_NOGDEFL` becomes worth 0.0711″ and `FLG_NOABERR` 20.8557″ (and the 66386/65810 pair, identical under `FLG_TRUEPOS`, separates by up to 0.128869″ without it). Never reason about one of these flags without the others.
- **The position-flag word has three levers, and the serving one is a mutable global built before this package runs.** (1) `jhora.const.PLANET_POSITIONS_*`, read by `jhora.utils.set_flags_for_planet_positions()` on every call — the inherited default, and what moved between 4.8.6 and 4.8.7; (2) `drik.PLANET_FLAGS`, built from (1) at drik's import time and read at call time by `drik.sidereal_longitude`, `drik.dhasavarga` (hence `charts.rasi_chart`) and `drik.planets_in_retrograde` — this is what actually serves; (3) `jhora.const._TROPICAL_MODE`, which makes `drik.sidereal_longitude` ignore `PLANET_FLAGS` entirely and compute with `FLG_SWIEPH + FLG_SPEED` (tropical, ~24° away) — a pinned word that is bypassed is not a pin. `utils.apply_position_flags()` checks the declaration against its own decomposition (0) and against the masking invariant (0b), then **compares (1) against the declaration and hard-fails at import on any disagreement**, then pins (2) and reads it back, then verifies (3). The comparison against (1) is the whole point: pinning alone would silently absorb a dependency that changed its mind, which is the exact failure the 576 move demonstrates — and note that absorbing it is *also* why the declaration had to move to 65810 rather than keep 66386 and pin over the top; an equality you relax to tolerate a disagreement is no longer a tripwire. Scope, stated so it is not discovered later: this is the **planetary position** word only — the ascendant and Bhava-Chalit cusps come from `swe.houses(jd_utc, lat, lon, b"P")` (chart.py), which takes no flag word, and sunrise/sunset use pyjhora's separate `drik.RISE_FLAGS`/`SET_FLAGS` (measured **897 / 898, identical in 4.8.6 and 4.8.7**, so unmoved — and now declared in their own right, see the next bullet). Contract: `tests/test_position_flags.py`; extend it, never weaken it.
- **Sunrise/sunset convention word: DECLARED in `bphs_core/utils.py` (`RISE_FLAGS = 897`, `SET_FLAGS = 898`) and never inherited.** Every panchanga/electional limb is anchored to the day boundary — `drik.sunrise`/`sunset`/`moonrise`/`moonset`, and through them tithi, nakshatra, yoga, karana, the thirty muhurtas and the whole muhurat/lagna-shuddhi scan. Each of those calls `swe.rise_trans(..., rsmi=<word>, flags=PLANET_FLAGS)`, where the **`rsmi` word is DISTINCT from the ephemeris `flags` word `POSITION_FLAGS` pins** and decides *what counts as sunrise*: disc centre vs lower limb, true (geometric) vs apparent (refracted) horizon, rise vs set. The declared convention is swisseph's `BIT_HINDU_RISING` (896) — the Sun's disc **centre** at the **true, unrefracted** horizon (geocentric, no ecliptic-latitude correction), i.e. the classical *madhya-bimba* Hindu sunrise, matching `drik.sunrise`'s docstring and drik's own "geometric, true sunrise/set, refraction not considered" comment. This geometric disc-centre (Surya-Siddhanta) convention is **owner-ruled 2026-08-17**; the civil/almanac upper-limb-at-refracted-horizon convention is a real alternative a secular ephemeris reports, but the choice between them is settled, not an open question. It is load-bearing: **measured at Colombo 2024-03-20, clearing `BIT_NO_REFRACTION` moves sunrise 147.99 s (06:18:01 → 06:15:33)**, so the convention affects every limb measured from the day boundary. **Two levers, not three** — there is no runtime bypass here analogous to `_TROPICAL_MODE`: (1) `jhora.const.RISE_SET_HINDU_RISING`/`_USE_REFRACTION`/`_USE_DISC_CENTER_FOR_RISING`, the three globals `jhora.utils.set_flags_for_rise_set()` reads (the inherited default; flipping each moves the word to 769/770, 385/386, 9089/9090 respectively); (2) `drik.RISE_FLAGS`/`SET_FLAGS`, the mutable serving globals built from (1) at drik's import time. `utils.apply_rise_set_flags()` **compares (1) against the declaration and hard-fails at import on any disagreement** — the dependency-bump tripwire, exactly as `apply_position_flags` does — then pins (2) and reads both back. Pinning is behavior-preserving on the frozen resolve (the library already builds 897/898); the declaration is what makes a future `RISE_SET_*` default-flip legible instead of a silent day-boundary shift no electional golden could see. Contract: `tests/test_rise_set_flags.py`; extend it, never weaken it.
- **Two planet-id spaces exist and they are NOT interchangeable — graha names cross module edges, never raw integer body ids.** pyjhora numbers the grahas by the order of `utils.PLANETS` (Sun 0, Moon 1, **Mars 2, Mercury 3, Jupiter 4, Venus 5**, Saturn 6, Rahu 7, Ketu 8) — the space its chart *outputs* use (`charts.rasi_chart`, `drik.dhasavarga`, `drik.planets_in_retrograde` return these ids). swisseph numbers bodies Sun 0, Moon 1, **Mercury 2, Venus 3, Mars 4, Jupiter 5**, Saturn 6, Uranus 7, Neptune 8, node 10/11 — the space `swe.calc_ut` and `drik.sidereal_longitude` *consume* (pyjhora's own docstring: "The sequence number of 0 to 8 for planets is not followed by swiss ephemeris"). Sun, Moon and Saturn coincide, which is what lets a conflation survive spot checks: pass a PLANETS index to `sidereal_longitude` and a third of the output is right while Mars silently computes **Mercury**, Mercury→Venus, Jupiter→Mars, Venus→Jupiter, Rahu→**Uranus** and Ketu→**Neptune** — wrong celestial bodies, not small errors (measured 2026-07-31: index 7 returned 40.7495° = Uranus where the true node stood at 305.5916°). This shipped twice, in the transit endpoint's planet table and in the electional scorer's lord/malefic ids. The standing rule: per-body positions are requested **by graha name** through `utils.graha_sidereal_longitude(jd_utc, name)` — the single sanctioned boundary, which resolves names against explicit `swe.*` constants (Rahu = the declared `LUNAR_NODE_BODY`, Ketu = Rahu + 180°) — and `drik.sidereal_longitude` / `swe.calc_ut` are called **nowhere outside `bphs_core/utils.py`**. Never rebuild an id table from `enumerate(utils.PLANETS)`. A compute failure inside the lord/malefic scoring limbs **propagates** — it is never swallowed into a neutral-looking score (a swallowed `except: pass` there once reported wrong astrology as fine). Contract: `tests/test_graha_body_ids.py` — per-graha positive assertions against independent computations, plus an AST scan (parses code, cannot be defeated by comments/strings) that fails any new `sidereal_longitude`/`calc_ut` call site outside the boundary. Extend it, never weaken it.
- **Served clock times use ONE minute-rendering convention — TRUNCATE to the minute the instant falls in — via `bphs_core/muhurat.py::float_hours_to_hhmm`, byte-identical to pyjhora's `utils.to_dms(...)[:5]`.** Every HH:MM a muhurat/panchanga day serves — sunrise, sunset, moonrise, moonset, the 30 muhurtas, the auspicious/inauspicious/chogadiya/rahu-kala windows, and the tithi/nakshatra/yoga/karana end-times — must render on the same clock, because a period BOUNDARY is frequently the SAME instant as an event it is derived from: the night-muhurta division opens at exactly `drik.sunset(...)[0]`, and the chogadiya day/night split and one weekday's rahu-kala edge land on sunset too. `float_hours_to_hhmm` used to ROUND to the nearest minute while pyjhora's own `to_dms` (which formats every drik-computed time string this service then slices `[:5]`) TRUNCATES, so an 18:17:58 sunset served as `sunset` "18:17" but opened its night muhurta at "18:18" — register #176. The fix renders the helper on the library-native truncation (round the residual to whole seconds, then truncate the minute — exactly `to_dms`), and routes sunrise/sunset/moonrise/moonset through it on the float `[0]` so the event and the boundary derived from it pass through one formatter and cannot disagree. The BPHS-accurate value is the underlying float and is untouched by the choice; only the minute LABEL is pinned, and it is pinned to the convention already governing the 10 drik-string categories rather than introducing a second one. Convention chosen over nearest-minute rounding deliberately: rounding would (a) diverge from those 10 library-native categories, re-creating the same disagreement at the chogadiya/rahu-kala boundaries that also coincide with sunset, and (b) shift the minute-rendered sunrise the hora-lord limb consumes, drifting a computation this display fix must not touch. Contract: `tests/test_muhurat_deep.py::TestSingleClockConvention` locks `float_hours_to_hhmm ≡ to_dms(...)[:5]` (so the `[:5]` sites this helper does not itself format can never drift from it) and the served field==boundary invariant across a spread including the half-minute-second band where truncate and round diverge. Never reintroduce a second rounding convention for a served time.
- Origin lesson: this codifies a real ascendant-timezone bug found and fixed in this repo; the rule above is self-contained and does not require any external context to apply correctly.

## Determinism

This is an electional/astronomical engine — outputs must be reproducible.

- Astronomical outputs are golden-value tested against synthetic, non-personal reference charts. `tests/test_calc_regressions.py` is the canonical pattern: synthetic charts only, invariant assertions (e.g. Chart longitudes equal a direct UT swisseph computation for any non-zero timezone). **No personal or family birth data ever enters tests, fixtures, plans, or committed reference files** — synthesize charts or assert invariants instead.
- Any change to a calculation module (`bphs_core/*`) that could shift a numeric output requires a golden/regression test that first failed (per the parent workspace's test-first non-negotiable) and pins the expected value. "It runs" is not sufficient evidence for an accuracy engine.
- Fail-closed determinism governs the electional (muhurat / lagna-shuddhi) pipeline: a limb that cannot be computed must never resolve silently to "fine / auspicious". `tests/test_panchanga_fail_closed.py` is the contract; extend it, never weaken it. One-sentence mandate: a missing limb means visibly degraded, never a clean pass.
- **Which failure mode a limb takes is DECLARED, not uniform — project decision 2026-08-17: a limb failure that can change WHICH time is recommended must RAISE (`bphs_core.muhurat.MuhurtaLimbError`, naming the limb); only genuinely supplementary limbs may degrade, and only behind an explicit per-limb flag.** Every limb in `bphs_core/muhurat.py` used to answer failure the same way — a log line plus a default, a dropped window, or a `None` — so a day assembled from a broken frame was indistinguishable on the wire from a day assembled from a good one. The line is drawn from what `bphs_core/lagna_shuddhi.py` actually reads, not from how central a limb sounds: `_candidate_minutes` builds the ENTIRE candidate-minute set from `auspicious_muhurtas` + the favourable `chogadiya` windows, and `_score_instant` hard-excludes on the Rahu/Yamaganda/Gulika windows and, per activity, on Durmuhurtam/Varjyam, the Vishti karana, the eclipse flag and the adhika-maasa flag, scores on the tithi/yoga names, and derives the hora lord from the served `sunrise`. A dropped window or an unverifiable flag in any of those does **not** make the engine cautious — it deletes a veto or a candidate, silently. Limbs with **zero** reads in that pipeline (moonrise/moonset, the four panchanga end-times, the amrita windows, the 30-muhurta display table, the personal-balam strings) are the ones that may degrade; they name themselves in the additive `degraded_limbs` field, from which the summary `degraded` bool is DERIVED so the two cannot disagree. Corollaries: a NAME and its END TIME are classified separately even when one call yields both (`drik.tithi`/`karana`); a veto flag that resolves to `None` and asks its reader to fail closed is the same silent-drop shape as an omitted window, so `panchaka_free`/`is_eclipse_day`/`is_adhik_maasa` are now always answered here (they stay nullable on the wire, and the consumer-side null-vetoes-too gate stays, for payloads from any other producer); `hard_gate_failed` is retained on the wire and always False from this service, because the three limbs that set it now raise. **At the HTTP boundary a raised `MuhurtaLimbError` is surfaced by the synchronous routes as a structured `422`** — `{"detail": {"code": "muhurat_limb_error", "limb": <name>, "target_date": <YYYY-MM-DD>, "message": <str>}}`, via the `@app.exception_handler(MuhurtaLimbError)` in `app/main.py` — never an opaque `500` and never a `200` with an empty/partial muhurat list. `422` (not a 5xx) matches this service's existing "well-formed request the engine cannot fulfil" convention (date-range caps, member counts, invalid activity); the raise site already logs at ERROR with the traceback, so no observability is lost. The handler is registered against the exception class, so it also covers the synchronous lagna-shuddhi routes; it does **not** fire on the async scan-job path, which captures the same failure into the job's `error` field on its background thread (`app/jobs.py`). Contracts: `tests/test_muhurat_limb_failure_modes.py`, which discovers the `drik` entry points by **parsing** the module and fails any limb carrying no recorded classification, and `tests/test_muhurat_limb_error_route.py` for the route-level envelope — extend them, never weaken them.
- **A rise/set limb's failure mode is unreachable through `except` alone — the library returns a SENTINEL, not an exception.** Measured 2026-08-17 against the real Swiss data files: at latitude 78.0 on 2026-06-21 `drik.sunrise`/`sunset` returned `-59069099.0` rather than raising, `float_hours_to_hhmm` reduced it modulo 24, and the day was served as sunrise **"13:00"**, sunset **"13:00"**, `degraded: False`, `hard_gate_failed: False`, with 30 muhurtas, 16 chogadiya windows and three **zero-width** absolute-veto windows — a complete, clean-looking recommendation resting on a day frame that does not exist. Every rise/set float is therefore range-validated (`muhurat._event_hours`) before it is rendered: over 5980 mid-latitude samples (lat −66..66 step 6, 5 longitudes, 13 dates, all four limbs) the value ran min 0.0033 h / max 26.0241 h with **none** outside `[0, 48)` — the bound is one full wrap wide because an event legitimately lands after local midnight — while of 600 polar sunrise/sunset samples (|lat| 67..89, 3 longitudes, 10 dates) the 228 real events sat inside it and all 372 no-rise cases were large negatives in `[-59073516.0, -59064996.0]`. Generalise: when a foreign call reports "no such event" **in band** rather than by raising, an exception handler around it is dead code, and the guard has to read the value.
- **A dependency's wire shape is pinned by a test that drives the REAL callee — a fabricated fixture validates a fiction, and can pin the defect itself as expected.** Measured 2026-08-05: the 30-muhurta limb served an **empty list behind HTTP 200 on every single request**, with `degraded` reporting `False`. `drik.muhurthas()` returns 30 entries shaped `(name, auspicious_flag, (start_hours, end_hours))`; the parser sniffed `isinstance(entry, tuple) and len(entry) >= 2` — which the real 3-tuple satisfies — then read `entry[0]`, the *name*, as a float hour, so `float_hours_to_hhmm` raised `TypeError` on every iteration and a bare `except Exception` wrapped around the whole loop turned it into a log line. The suite could not see it because the test monkeypatched `drik.muhurthas` with `(6.0, 6.8)` 2-tuples the library has never returned, and then **asserted the empty list as the expected result** — encoding the bug as correct behaviour, so a correct implementation would have failed the suite. Five standing rules follow, and they generalise to every `drik.*`/`swe.*` return this engine parses:
  - Every limb that parses a third-party return carries at least one test driving the **unpatched** callee, asserting the **structured** result — how many entries, which names, what boundaries. `assert result != []` passes on a single garbage entry and is not evidence.
  - A broad `except` wraps **only** the foreign call, never the parsing of its result. Use `try/except/else`: the environmental failure mode (sunrise/sunset at extreme latitudes) degrades visibly inside `except`; the parse sits in `else`, where a contract break **raises** instead of being served as silence. An empty field a consumer cannot distinguish from a real answer is worse than a 500 — the 500 is visible and recoverable.
  - Monkeypatching a foreign function is legitimate to simulate **failure**, never to invent a **success** shape. If a test needs a success shape, it must be the shape the library actually produces, pinned by a sibling test that reads the library directly.
  - Parse strictly and reject an unexpected **count** as well as an unexpected shape: positional labelling (`_MUHURTA_NAMES[i]`) silently mislabels every window if the library's entry count ever moves, and `idx % 30` wrapping hides exactly that.
  - **Where a label is assigned by index, pin the dependency's own ORDER — a count check and a shape check are both blind to a reorder.** This is the same defect class as the two planet-id spaces above: positional identity carried across a library boundary. `drik.muhurthas` names its entries from the insertion order of `jhora.const.muhurthas_of_the_day` while deriving the time slots from the index, so discarding `entry[0]` and labelling slot *i* with `_MUHURTA_NAMES[i]` is correct only while that key order holds. Measured 2026-08-05: transposing **two adjacent keys** — the shape a real upstream change takes — served window 1 as "Ahi" where the library called it `mithra` and window 2 as "Mitra" where the library called it `aahi`, with `degraded: False`, every entry still matching the contracted 3-tuple, the count still 30, the windows still tiling the day contiguously, and **all 82 tests then covering this limb green** (`tests/test_muhurat_deep.py` + `tests/test_panchanga_fail_closed.py`). Comparing served labels to `_MUHURTA_NAMES` cannot catch it — that is the constant they were copied from, so the assertion is tautological with respect to ordering. The library's `(name, flag)` sequence is therefore pinned position by position (`tests/test_muhurat_deep.py`, `_MUHURTA_POSITIONAL_SIGNATURE`), together with the label each slot must serve under. **Pin the dependency's own names, not a derived signature.** The auspicious flags are the tempting choice because they carry no transliteration risk, but measured on this table they catch only **11 of the 29** adjacent transpositions (37.9%) and 189 of the 435 total (43.4%) — the flag column takes only **two** values across the 30 positions, so most neighbouring pairs share one, and a transposition of two **same-flag neighbours** is invisible to it — whereas the names, being dict keys, are unique and so move under **every** transposition (29/29 and 435/435). A flags-only pin catches the `aahi`/`mithra` swap above and stays green on `rudra`/`aahi`, which both carry flag 0 and mislabels two windows just as thoroughly. Every count in this paragraph is **computed** from the pinned table by `tests/test_muhurat_deep.py::TestSignatureColumnDiscrimination`, which asserts each one: cite what that test computes, and never re-count the table by hand into prose here. That last clause is itself a correction — this sentence used to gloss the mechanism as a hand-counted run of identical flags, the run length was wrong, and it had been copied into two further places before anyone re-*derived* it instead of re-*reading* it. The flag is kept as a *second*, spelling-independent signature, because it is the only column that moves when a key holds its position while its auspiciousness value changes. The cost of pinning names is a red test on a benign upstream respelling — which lands only inside a deliberate dependency bump this repo already requires to be handled deliberately, and which is the moment the label correspondence should be re-derived anyway. Production keeps discarding `entry[0]` rather than comparing it (`bphs_core/muhurat.py`, comment at the parse loop): comparing there would make a harmless respelling a 500 on every request, and the served path cannot tell a respelling from a reorder without the same pinned table.

## API contract stability

Endpoints are versioned under `/v1/*` (chart, strength, dashas, yogas, transits, special-points, profile, muhurat, muhurat.lagna-shuddhi, muhurat.family-lagna-shuddhi, compat — all POST; plus GET /source, GET /healthz). Response models live in `app/schemas.py` and are the wire contract with the caller.

- A backward-incompatible change to any request or response schema (removed/renamed field, changed type or units, tightened enum) requires a version bump (a new `/vN/` path) — never an in-place mutation of `/v1`. Additive optional fields are fine. Never break `/v1` in place; the caller pins to it.
- `/source` (authenticated) returns `{source_url, commit}`, where `commit` is the cache-invalidation key resolved by `_resolve_version()` in `app/main.py` (called once at import, cached as the module-level `_COMMIT`): `GIT_COMMIT` env (baked at image build, Dockerfile `ARG`/`ENV`) is authoritative; the source-content hash `src-<sha256[:16]>` is the dev fallback; the literal `unknown` is the non-cacheable sentinel. Never substitute a fabricated commit for `unknown` — the caller keys its result cache on this value, and a wrong key serves stale astronomy.

## HTTP service hardening

- Auth fails **closed**. `app/auth.py` refuses to start (the `raise RuntimeError` in its module-level `if _TOKEN_REQUIRED:` block, on the verdict of `_token_weakness_reason`) when `ENVIRONMENT` is outside `{development, local, test}` and `CALC_SERVICE_TOKEN` is missing, a known placeholder, or shorter than 16 characters. `ENVIRONMENT` defaults to `production` precisely so a missing value is fail-secure — that default lives in `environment()` in `app/deployment.py`, the single definition both fail-closed guards read; `app/auth.py` only caches its verdict at import as `_TOKEN_REQUIRED`. Rules: (1) never change that default to a permissive value; (2) never add a code path that serves an authenticated endpoint when the token is unset outside dev; (3) token comparison uses `hmac.compare_digest` — never `==` (timing attack surface) — in `require_token`; (4) the app secret travels in its own header, `X-Calc-Service-Token`; `Authorization` is reserved for the platform's OIDC identity token — never merge the two into one header (`require_token`'s `x_calc_service_token` Header parameter, and the rule stated in its docstring).
- `/healthz` is intentionally unauthenticated (liveness probe, no sensitive body — the `@app.get("/healthz")` route in `app/main.py` deliberately carries no `require_token` dependency) — keep its body limited to a status string plus an `ephe_loaded` boolean; never add sensitive data to it.
- Validate every endpoint at the schema boundary: latitude `ge=-90, le=90`, longitude `ge=-180, le=180`, timezone `ge=-12, le=14`, and every float field carries `allow_inf_nan=False`. Hard rule on the NaN subtlety: `ge`/`le` alone do **not** reject NaN (IEEE-754 comparisons with NaN are always false), so a NaN value passes range checks and flows into `swe.houses()` / `drik.Place()`, producing a finite-but-wrong chart. Every new float input field **must** set `allow_inf_nan=False` (see `app/schemas.py` and the contract in `tests/test_coord_bounds.py`). Also bound expensive-scan inputs — date-range caps (`MAX_*_DAYS`), `step_seconds >= 60`, family members `2..6` — so a single request cannot force an unbounded compute.
- The 422 handler must never regress into a 500: `_validation_exception_handler` in `app/main.py` sanitizes non-finite floats and exception objects before serializing, through its `_sanitize_for_json` helper. Keep validation rejections as well-formed 422s.
- Keep-alive / stale-connection handling is a two-sided contract. Server side: uvicorn runs with `--timeout-keep-alive 75` (Dockerfile CMD) so idle pooled connections aren't closed out from under a client mid-send (which otherwise surfaces as an `httpx.ReadError`) — keep this value at or above any upstream/load-balancer idle timeout. Client-side retry of the residual race is the caller's responsibility, not this repo's. If you change one side of this contract, reason about both.
- CORS `allow_origins` is driven by the `ALLOWED_ORIGINS` env var (read at import into `_ALLOWED_ORIGINS` in `app/main.py` and passed to `app.add_middleware(CORSMiddleware, allow_origins=…)`; a bare `*` is rejected there at import) and is empty by default — this is a server-to-server service. Do not introduce a wildcard `["*"]` origin.

## Async scanning design — the GIL trade-off

The service runs single-threaded under Python's GIL. A wide date-range electional scan (up to `MAX_LAGNA_SHUDDHI_DAYS` days) can occupy a request thread for its full duration, starving concurrent chart requests and risking the caller's connection/proxy timeout. The async submit/poll endpoint (`app/jobs.py`) solves this: it hands back a job id immediately and runs the scan on a small background thread pool, decoupled from the submitting request's lifecycle.

**What it solves:** the connection timeout problem for long-running scans. The HTTP call that submits a scan returns right away; the caller polls for results separately.

**What it does NOT solve:** GIL contention. The background thread pool and the sync `/v1/*` handlers share the single process's GIL. CPU-bound scans and interactive chart requests still contend, degrading latency of concurrent sync handlers while a scan runs. The design's own module docstring in `app/jobs.py` already acknowledges this. The thread pool is deliberately kept small (`max_workers=4`) to bound the contention, and the design ensures submissions and polls (which only touch a lock-protected dict) remain O(1) and never wait on scan compute.

**Why the trade-off is accepted:** (1) it solves a real, reported problem (caller connection timeouts); (2) residual latency degradation is bounded by `max_workers=4` and occurs only during a scan; (3) the caller has a durable synchronous path (`/v1/muhurat`, `/v1/muhurat.lagna-shuddhi`) as the canonical route — the async variant is strictly additive; (4) job state is process-local and does not survive a restart, acceptable because the sync path remains intact; (5) no evidence of a breached latency SLO today.

**If the decision reverses:** should a latency SLO be breached or the caller profile change to many concurrent scans, isolate scan CPU in a subprocess or `multiprocessing.Pool` to eliminate GIL contention. This would require separate infrastructure (subprocess management, IPC, cleanup on restart) — acceptable at that point, but not now.

## Supply-chain & image hygiene

- The shipped Docker image installs from the committed, frozen lockfile: `Dockerfile` copies `pyproject.toml`/`uv.lock` into the build context and runs `uv sync --frozen` (once for third-party deps, once for the project itself), so the deployed dependency set is provably the tested one — there is no floating `pip install -e .` step left. Keep it this way; never reintroduce an unpinned resolve at build time.
- **Every external image is a digest-pinned `FROM`, never a `COPY --from=<registry image>` — and the two halves of that rule are independent.** Both Dockerfiles used to install the build tool with `COPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /bin/`, defended by a comment arguing a floating tag was fine for an installer. It was not, for two separate reasons, and fixing either one alone leaves the image floating. (1) `:latest` is re-resolved on **every** build, so two builds of the same commit can install different bytes with nothing recording which shipped — in an image whose every other input is pinned (`.python-version`, `uv.lock` under `uv sync --frozen`, the ephemeris files to sha256). (2) **`COPY --from=<registry image>` is invisible to dependabot**, whose `docker` ecosystem parses `FROM` instructions only — so an image reached that way is never bumped for a disclosed CVE and never reported as outdated *however carefully it is pinned*. Pinning the digest on the `COPY` would therefore have traded a mutable reference for a frozen, unwatched one, which rots quietly instead of loudly. The required shape is `FROM <image>:<version>@sha256:<digest> AS <name>` + `COPY --from=<name>`: the digest makes the build reproducible, the **version tag** makes it maintainable (dependabot compares tags to decide a newer release exists, and a digest-only reference is frozen forever), and the `FROM` makes it visible. Resolve a digest with `docker buildx imagetools inspect <image>:<version>` — take the multi-arch **index** digest, never a per-platform one, and never hand-write a digest. The bare official base (`python:3.11-slim`) is the one exemption from the digest requirement, because its tag is separately asserted against `.python-version` by `ci/tests/test_environment_convergence.py` — a floating tag there is still rejected. Contract: `ci/tests/test_dockerfile_image_pins.py`, which enumerates the Dockerfiles from `git ls-files` (so a third one is covered by construction) and floors what it examined; extend it, never weaken it.
- Dependency CVE scanning and `dependabot.yml` are both live: `.github/workflows/test.yml` runs `pip-audit --requirement requirements.frozen.txt` against the frozen lock, and `.github/dependabot.yml` watches `pip` + `github-actions` + `docker` (the base images **and** the digest-pinned `uv` builder stage, now that it is a `FROM`) weekly. `pyswisseph` is a hard-pinned compiled C extension that parses ephemeris files — keep it covered by this scan; this is baseline hygiene expected of a public AGPL project.
- **Both astronomy dependencies are EXACT pins — `pyswisseph==2.10.3.2` and `pyjhora==4.8.7` — because both can move a served number on a resolve.** `pyjhora` owns all three lunar-node levers **and all three position-flag levers** (see Engine conventions), so an open bound there was a path for every Rahu and Ketu to move up to 1.98° with no change in this repository; `pyswisseph` is the ephemeris itself. A dependabot bump to either is therefore **not routine**: it lands in its own reviewed change that re-runs the Swiss goldens, `tests/test_lunar_node_model.py` and `tests/test_position_flags.py`, and re-records goldens only deliberately. **`pyjhora` 4.8.7 was the measured, concrete instance of why: it changed the position-flag word from 66386 to 65810 and the goldens could not see it** (the difference is output-inert under `FLG_TRUEPOS`), so `tests/test_position_flags.py` was the only thing in this repository that failed on it. **That bump was taken on 2026-08-21** and the frame change decided deliberately in its review rather than absorbed — measured evidence at the time: 41 collection errors from the applier, zero golden movement. Two further 4.8.7 changes surfaced only because that review read the wheel diff, since upstream's release note says nothing but "See README.MD" and the real changelog lives in `drik.py`'s module docstring: (a) `drik.planets_in_retrograde` stopped filtering the lunar nodes out (`if p not in [const._RAHU, const._KETU]` → `[const._SUN, const._MOON]`), so the library now reports the nodes' computed direction — measured 27.75% *direct* — which would have flipped Rahu/Ketu retrogression on roughly a quarter of charts had `PERPETUALLY_RETROGRADE_GRAHAS` not been declared; and (b) `drik.dhasavarga`'s `set_rahu_ketu_as_true_nodes` default moved from the literal `True` to `None`, so the node model's third lever now defers to the constants this repo pins (see the lunar-node bullet). Neither moved a served number. **The standing rule is unchanged: a bump to either astronomy dependency is not routine, and the wheel diff is read — not just the release note — because the flag-word move appears in neither the release note nor the changelog.** When changing a pin, run `uv lock` in the same commit — `uv sync --frozen` fails the image build on any `pyproject.toml`/`uv.lock` drift, and `tests/test_lunar_node_model.py` asserts the two agree.
- **One resolve, validated where it ships — and the INTERPRETER is part of the resolve.** Every environment here (both CI test jobs, the accuracy job, `Dockerfile`, `Dockerfile.test`, a developer checkout) installs `uv sync --frozen` on the interpreter declared in **`.python-version`**, which is the single place that version is written; `actions/setup-python` reads it via `python-version-file`, `uv` reads it directly, and `ci/tests/test_environment_convergence.py` checks both Dockerfile base images against it. Never inline a Python version in a workflow, and never reintroduce `pip install -e ".[dev]"` — measured 2026-08-05, that floating install was resolving a set that differed from the shipped image on **13 packages that actually ship** (fastapi 0.141.1 vs 0.136.3, uvicorn 0.52.1 vs 0.48.0, anyio, certifi, cffi, charset-normalizer, click, idna, pytz, typing-extensions, websockets, annotated-types, annotated-doc), so the suite gating every merge certified a dependency set no deployment ran.
  - **Why the interpreter is not a detail: `uv.lock` FORKS on it.** Of 73 locked package names exactly two resolve to more than one version, both splitting on `python_full_version` 3.11: **`numpy` 2.2.6 / 2.4.6** and **`timezonefinder` 8.2.0 / 8.2.5**. So "the locked set" does not name a single set of versions until the interpreter is fixed, and a base-image tag edit silently changes what installs while `pyproject.toml` and `uv.lock` stay byte-identical. `.github/dependabot.yml` watches the `docker` ecosystem weekly, so that edit arrives on its own; it is **not routine** and belongs in its own reviewed change.
  - **Neither forking package is reached by the served compute path. This is MEASURED, on both — and it is written here because an earlier revision of this file asserted the opposite for `numpy`.** That claim was that Shadbala/Bhavabala run through `jhora/horoscope/chart/strength.py` (`np.floor`/`np.rint`/`np.around`) and the varga tables through `jhora/horoscope/chart/ashtakavarga.py` (`np.asarray(...).sum(axis=0)`/`np.multiply`). **It is false: this repo computes Shadbala, Bhavabala and Ashtakavarga itself, in `bphs_core/strength.py`, which contains no numpy at all.** `bphs_core` imports only `drik`, `charts`, `const` and `utils`; `charts.py` in turn imports only `math`/`drik`/`const`/`utils`/`house`. Those two jhora modules are vendored-but-never-imported. Measured 2026-08-05 by wrapping `np.floor`/`np.rint`/`np.around`/`np.sum`/`np.asarray`/`np.multiply`/`np.array`/`np.where` with counters, **with the instrument proven live before any zero was read** (direct calls moved every counter, and a direct call into jhora's own `get_ashtaka_varga` moved `asarray` 0→1, so a zero means absence and not a broken probe): **every one of the 15 `/v1/*` routes, plus `/healthz` and `/source` — 17 of 17 answering 200/202, with the two async scan jobs polled through to terminal status so their worker-thread compute was counted too — left every counter at 0**, and the full 697-test suite recorded **2 numpy calls in total, both at import time and none during execution**. `jhora.horoscope.chart.strength` and `jhora.horoscope.chart.ashtakavarga` were never in `sys.modules`. (`timezonefinder` is the same shape, measured the same way: `jhora/utils.py` imports it at module load and calls it only from `get_place_timezone_offset`, which a full `Chart` compute invokes **0** times, since place and timezone arrive on the request.)
  - **numpy's entire measured footprint here is ONE import-time expression** — the `house_owners = np.where(np.array(house_strengths_of_planets).transpose()==_OWNER_RULER)[1].tolist()` assignment in `jhora/const.py`, cited by symbol because it sits in a vendored dependency whose line numbers move with the pinned version: an integer index extraction over a constant table, consumed by `jhora/horoscope/chart/house.py`. Those are the two calls counted above. Whether crossing the fork perturbs it was **measured, not assumed either way**: the identical expression on the identical table returns `[2, 5, 3, 1, 0, 3, 5, 2, 4, 6, 6, 4]` under **numpy 2.2.6 on Python 3.10** and under **numpy 2.4.6 on Python 3.11** — same values, same element types. So the interpreter pin is **not** justified as a numerics guard on `numpy`, and must not be re-argued as one.
  - **What the pin and its guard DO rest on: what the gating suite tests must be what ships.** Measured 2026-08-05, the floating install differed from the shipped image on the **13 packages** listed above — so the suite gating every merge, including the coverage gate that authorises the image build, was certifying a dependency set no deployment ran. That reason holds whatever numpy turns out to reach, which is the point: it does not depend on the claim above being true. Two further reasons the interpreter stays declared in exactly one place: the fork makes "the locked set" ambiguous while the interpreter floats, and **reachability is a property of today's code, not a guarantee** — the day any path here calls into jhora's own `strength`/`ashtakavarga`, or a new dependency puts float arithmetic on the served path, the fork stops being inert and nothing in the diff announces it. Keep all four accuracy-critical pins in `ci/tests/test_environment_convergence.py`, but keep the reasons distinct: `pyswisseph` and `pyjhora` are **measured** to move served numbers (see above — the 576 flag word, the 1.98° node); `numpy` and `timezonefinder` are pinned because dropping a pin the moment you cannot prove harm is a pin that fails open.
  - The contract is `ci/tests/test_environment_convergence.py`: it compares the **installed** distributions against what `uv.lock` resolves for the running interpreter (a measurement of the live environment, not a reading of config), asserts the interpreter equals `.python-version`, parses the workflows for any job that runs the suite without a frozen install, and parses both Dockerfiles' `FROM python:` tags. On the workflow arm it checks the **value**, not merely the presence, of each `actions/setup-python` `python-version-file` — a job pointing that key at any other filename is a second declaration of the interpreter wearing the right key's name — and its floating-install matcher covers pip's long form `--editable` as well as `-e` (measured: a step running `pip install --editable .` alongside a frozen install used to leave the check green). It prints and floors the number of things it compared, because "no differences found" is vacuously true of a comparison that examined nothing. Extend it, never weaken it. That enumeration is the *interpreter and dependency-set* half; the same file's uv-toolchain and lock-freshness arms are the sub-bullet below.
  - **The INSTALLER is part of the resolve too — CI's uv is pinned to the images' uv, and the lock's freshness is asserted separately.** Measured 2026-08-21, three CI steps installed the build tool with a bare `python -m pip install --upgrade uv` — `test.yml`'s `test` (the fast suite) and `swiss-ephemeris` (the accuracy gate) jobs, and `publish.yml`'s `test` job (which gates the image build) — floating to the newest release on the index per run, while `Dockerfile` and `Dockerfile.test` both install it from a digest-pinned `FROM ghcr.io/astral-sh/uv:<version>@sha256:<digest> AS uv` stage. So CI could resolve, sync and test the lock through a **different uv** than the one that builds the shipped image: the 13-package divergence's own shape, moved one layer out from the dependency set to the tool that installs it. All three now use `astral-sh/setup-uv` with an **exact** `version:` equal to the Dockerfiles' tag — never `latest` or `latest-known`, which re-resolve per run and are the thing being removed. **The equality is the maintenance mechanism, not a chore.** dependabot's `docker` ecosystem watches the Dockerfiles' `FROM` lines, while its `github-actions` ecosystem bumps an action's `uses:` ref and **never the value of an input to it** — so the workflow pin is watched by nothing, and a pin nobody watches is a pin that rots. The guard therefore **derives** its expected value from the Dockerfiles and pins no version literal of its own: a uv bump arrives on the docker schedule, the test reds on the disagreement, and the workflow pin has to follow inside that same reviewed change. (Deliberately the inverse of `ci/tests/test_dockerfile_image_pins.py::test_the_build_tool_is_installed_from_a_pinned_stage`, which asserts the *shape* and never a version precisely so it does not fail on the bumps it exists to enable — the two are complementary, not redundant.) The action is referenced by a **full release tag** rather than the bare major every sibling action here uses, because measured 2026-08-21 `astral-sh/setup-uv` publishes moving major tags for v1..v7 only while its current release is v10.0.1, so `@v10` does not resolve; `enable-cache` is set `false` because the action defaults it **on** for hosted runners and there was no uv cache here before — leaving it unset would smuggle a new Actions cache, and a new failure mode, into a change whose purpose is to remove a moving part.
  - **`--frozen` is blind to a STALE LOCK by construction, so freshness is a separate assertion.** `uv sync --frozen` installs `uv.lock` exactly as committed and never consults `pyproject.toml` — so a dependency-range edit committed without re-running `uv lock` is installed, tested and shipped as though it had not happened, silently, because nothing fails. Measured 2026-08-21 on a scratch copy of this project's own pair, one pin edited in `pyproject.toml` with `uv.lock` left byte-identical: `uv lock --check` **exit 1**, `uv export --frozen` **exit 0** (the silent pass), `uv export --locked` exit 2 — and all three exit 0 on the unmodified pair. Every job that installs the frozen lock therefore also runs `uv lock --check`. That is deliberately an **additional step, not a flip of the install to `uv sync --locked`**: the shipped `Dockerfile` installs with `--frozen`, and this file's own rule is to run the exact command CI runs rather than an equivalent, so putting the image and its gate on two different install paths would reintroduce a divergence in the act of closing one — and it keeps the guard's `installs_frozen` arm intact rather than teaching it a second spelling. It resolves offline (~1 ms) when the lock is fresh; ordering is not asserted, because a stale lock fails the job from either position. Contract: `ci/tests/test_environment_convergence.py` (sections 5 and 6); extend it, never weaken it.

## Tests & local gate

- Coverage gate is `fail_under = 92` (`pyproject.toml`, `[tool.coverage.report]`) with `branch = true` (`[tool.coverage.run]`) — cited by table rather than by line number, because the line numbers this used to give (`40,49`) were already wrong before anything moved them, and a citation that rots silently is worse than none. Do not lower it; new logic ships with tests that keep it green. (`publish.yml`'s comment no longer cites a stale "90"; `pyproject.toml` remains authoritative.)
- **Citations name SYMBOLS, not line numbers — and that convention is ENFORCED, not merely observed.** A `file:line` citation rots silently on the next edit *above* the line it names, and the rotted form is worse than no citation at all because it survives an eyeball check by landing on plausible-looking code in an unrelated function. Every citation in tracked prose therefore names the callee and, where the claim is about containment, the enclosing function that holds it. `ci/tests/test_stable_anchor_citations.py` re-derives its corpus from `git ls-files` on every run — Python comments and docstrings, YAML/TOML comment lines, whole Markdown, and **never code string literals**, which is what keeps the synthetic pytest node-ids in `ci/tests/test_pytest_collection_check.py` from being read as citations — and resolves four forms: the node-id `<path>.py::<symbol>`, resolved by **AST against the tracked target, never by grep**; a class docstring's `named by function in <path>:` block, whose bullets must name module-scope symbols and whose `enclosing → callee` bullets must additionally **CONTAIN** (the callee is verified to be called inside the enclosing body — both halves resolving separately is not the claim the arrow makes); the `<symbol>: …` opener of each test docstring under such a block; and the tree's one **SHA-pinned** historical citation, which is *verified at its commit* with `git cat-file` rather than exempted — hence the `fetch-depth: 0` on the jobs that run `pytest ci/tests/` (measured: a depth-1 clone cannot resolve the pinned commit, and the guard fails closed rather than skipping, so a shallow checkout reds the build instead of quietly disarming the check). A fifth assertion is what makes this **standing** rather than a snapshot of one sweep: any `file:line` in tracked prose that is not SHA-pinned is refused, so a fresh line-number citation reds on the commit that introduces it — the one moment its author can cheaply convert it. The guard floors its own inventory **per form**, because "every anchor resolves" is vacuously true of zero anchors and a regex that quietly stops matching is exactly how a guard dies green. **The Form B header is admissible at MODULE scope, and that is what brought the bare `# <ident>:` comment anchors under the guard.** They were the standing exclusion #2: genuine citations whose target module was stated only in English, so selecting them meant selecting every `word:` clause in every comment in the tree — measured over the corpus, that selector also returns `Additive:`, `False:`, `Measured:` and `navamsa:`, none of them citations. The fix is a machine-readable owner rather than a widened selector: a *module* docstring may now carry `named by function in <path>:`, and inside a file that opts in, a `# <symbol>: …` comment opener is a citation **by construction** (Form F), resolved by AST against the declared module on every run. A comment has no enclosing AST node to inherit an owner from, which is precisely why module scope was the missing piece and why Form C stays class-scoped — a method docstring already has one. `tests/test_compat_deep.py` (→ `bphs_core/compat.py`) and `tests/test_panchanga_fail_closed.py` (→ `bphs_core/lagna_shuddhi.py`) are the two declaring files; a scope declares exactly **one** subject module and a second declaration in the same docstring is a hard failure rather than a silently-ignored one, so an anchor into any other module is spelled in Form A. The cost is stated in each declaring docstring rather than discovered by whoever reds the build: in a declared file, prose may not be written in the `Word: …` shape. What the guard still deliberately does **not** read is enumerated in its module docstring — prose backtick mentions, comment anchors in files that declare **no** module, code string literals, and citations into vendored dependencies — because a documented boundary beats a false-positive storm. Floors are per form, and the module-declaration floor is the one set at **equality**: a floor counting class-scope and module-scope declarations together keeps clearing on the class-scope ones alone, so it would go on passing with Form F reading nothing. Contract: `ci/tests/test_stable_anchor_citations.py`; extend it, never weaken it.
- **No subprocess read may decode where the failure is invisible — and "add an `encoding=`" is NOT the rule.** `subprocess` in text mode decodes the child's output *inside subprocess*, on a reader thread on Windows, where a `UnicodeDecodeError` kills the thread instead of propagating and the caller is handed `stdout=None` with `returncode=0`. Measured on the locked interpreter (CPython 3.11.15, win32, cp1252) and reproduced on 3.12.10, against a child emitting bytes that are neither valid UTF-8 nor defined in cp1252: `text=True` → `stdout=None`; **`text=True, encoding="utf-8"` → `stdout=None`**; `errors="strict"` → `stdout=None`; `encoding=` alone (which implies text mode) → `stdout=None`; `universal_newlines=True` → `stdout=None`; `errors="replace"` → decodes, lossily; bytes mode then decoding in-process → a **catchable** `UnicodeDecodeError`. The second is load-bearing: **an explicit encoding does not remove the hazard, it only changes which codec dies on the reader thread**, so a guard that required an encoding and stopped there would have certified the broken shapes as fixed. What separates safe from unsafe is neither the codec nor the identity of the calling helper, but whether the decode *can raise where nobody is listening*. Two shapes are admitted: **(A) bytes mode**, where the caller decodes and the failure lands in its own frame — the shape for anything that SCANS content, because a scanner must refuse an unreadable input rather than scan a lossy rendering of it; and **(B) an explicit `encoding=` that resolves through `codecs.lookup` to canonical `utf-8`, paired with a non-raising `errors=`**, where the reader thread cannot die. **The codec restriction in (B) is not decoration**: shape (B)'s whole justification is that ASCII survives a lossy decode, and that is codec-specific — measured, one stray lead byte before an ASCII marker under `errors="replace"` leaves the marker intact under utf-8 but **destroys it under `cp932`, `cp936` and `utf-16`**, whose double-byte forms consume the following character. Single-byte codecs preserve ASCII too, but admitting them would reintroduce exactly the locale-flavoured decoding this rule exists to remove. **Refused** — the full list is registered in the guard as `_REFUSAL_GROUNDS` and this prose is checked against it, so it cannot fall behind the code: text mode with no explicit `encoding=` (the decode uses the **locale codepage**, so the verdict depends on the machine); an `encoding=` that `codecs.lookup` does not know, or that is not canonical utf-8 (see `cp932` above); a raising `errors=` policy, including the default `strict`; a **non-literal** value in any text-mode keyword, since text mode is decided by TRUTHINESS (`encoding or errors or text or universal_newlines`) and an unreadable value may well select it — **though only where nothing else settles the question, and the list over-claims without that qualifier**: a *definitely* truthy keyword answers it, so measured, `text=<expr>` beside a literal `encoding="utf-8", errors="replace"` classifies **lossy**, not refused (a bare `text=<expr>` is refused, and a non-literal `encoding=`/`errors=` always is, since those two are read directly rather than for their truth value); `subprocess.getoutput`/`getstatusoutput` and **`os.popen`** in any form, which decode with the locale and accept no encoding parameter at all; a `**kwargs` splat, a `*args` splat, or **more than one positional argument** — because `universal_newlines` is `Popen`'s 12th **positional** parameter and `run`/`check_output`/`call`/`check_call` forward `*popenargs` into it, so text mode can be selected with no keyword at all (measured: `run(cmd, -1, None, None, PIPE, PIPE, None, True, False, None, None, True)` returns a `str`, and a guard reading only keywords called that **safe**). Every one of those is the same principle: a guard that cannot prove its property fails closed. Shape (B) is crash-free but **lossy**, a semantic risk the guard cannot adjudicate, so every shape-(B) site is enumerated in `_LOSSY_DECODE_REGISTER` with the reason lossiness cannot change *that* site's verdict; the register is keyed by `(path, enclosing function)` resolved by AST and is **self-expiring** — an entry that no longer resolves to a lossy site fails as stale, so a justification cannot outlive the code it was written for. It vets the lossy *choice*, not helper identity: measured, `errors="strict"` inside a carefully written helper dies exactly as it does anywhere else. Contract: `ci/tests/test_subprocess_decoding.py`, which parses the AST (so a comment or string literal can neither satisfy nor defeat it), resolves the ordinary aliasing forms (`import subprocess as x`, `from subprocess import run as y`, `from subprocess import *`, `sp = subprocess`) while **enumerating the forms it cannot reach in its own KNOWN BOUNDS** rather than claiming to catch them, cross-checks its text-mode rule against the **real engine**, and floors its inventory per form. Extend it, never weaken it.
- **A guard that enumerates the working tree must ask git for it in a form that cannot be re-encoded.** `ci/check_pytest_collection.py` used `git ls-files --cached --others --exclude-standard` without `-z`. Measured against a fixture repo holding a `pytest.ini` carrying `addopts = --collect-only` under a directory whose name is non-ASCII: the enumeration returned `'"caf/303/251/305/241/pytest.ini"'` and the checker reported **CLEAN**. Two faults compounded — `git ls-files` **quotes** a path holding non-ASCII bytes (`core.quotePath` defaults on), wrapping it in literal double quotes and rendering each byte as a backslash-octal escape, so the basename read back as `pytest.ini"` and matched nothing; and the enumeration's `.replace("\\", "/")`, present to normalise Windows separators, then rewrote those escapes into a fabricated `caf/303/251/305/241/` directory tree. That is the exact fail-open the checker exists to close, hidden behind a path component. `-z` disables quoting entirely and NUL-separates the records, so a path arrives verbatim whatever bytes it holds and whatever `core.quotePath` says — the shape `ci/tests/test_dockerfile_image_pins.py::tracked_dockerfiles` already used. The separator rewrite is **removed**, not kept: git emits `/` on every platform, so it had nothing left to do but corrupt a literal backslash in a POSIX filename, and records are no longer stripped either because with `-z` they are exact and a leading or trailing space is a legal filename character. An undecodable listing answers `None` — "git cannot answer" — which is not a silent pass: the caller falls back to `_walked_paths` and the tree is still scanned.
- **Two ephemeris runtimes, both exercised in CI (CALC-1).** `test.yml`'s `test` job and `publish.yml`'s `test` job run with **no** Swiss data files, so swisseph silently substitutes its built-in Moshier analytical ephemeris: line/branch coverage is identical to a Swiss run, but numeric values differ. `test.yml`'s **`swiss-ephemeris`** job runs the **same suite against the real Swiss data files** — `ci/fetch_swiss_ephemeris.py` fetches the four checksum-pinned files listed in `ci/swiss_ephemeris.json` into `data/ephe`, then pytest runs with `REQUIRE_SWISS_EPHEMERIS=1`.
  - Accuracy-sensitive golden values belong in `tests/goldens/swiss_ephemeris_goldens.json`, asserted by `tests/test_swiss_ephemeris.py` at 1e-6 deg. That tolerance is far tighter than the Swiss/Moshier divergence on purpose, so the goldens themselves cannot pass on the fallback engine. Elsewhere in `tests/`, keep tolerances valid under **both** runtimes.
  - **There are TWO accuracy corpora and they are not interchangeable. Know which one you are touching before you touch it.**
    - `tests/goldens/swiss_ephemeris_goldens.json` (asserted by `tests/test_swiss_ephemeris.py`) is **engine-recorded and regression-only**: expected and observed come from the same code, so it proves self-consistency and *nothing about correctness*. It cannot detect that a value was wrong the day it was recorded. Re-recording it (`UPDATE_SWISS_GOLDENS=1 python -m pytest tests/test_swiss_ephemeris.py`, refused when `CI` is set) is the sanctioned response to a **deliberate** dependency/data bump.
    - `tests/goldens/independent_reference_corpus.json` (asserted by `tests/test_independent_reference_corpus.py`, recorded by `ci/fetch_reference_corpus.py`) is **externally sourced**: the planetary longitudes and the Moon's osculating node come from **NASA/JPL Horizons** — DE441, evaluated by JPL's own code on JPL's servers, entirely outside this repo's dependency set — with the per-field source and the query URLs recorded verbatim in its `provenance` block. Three layers, each with a *measured* tolerance: L1 apparent longitudes at **1 arcsec**, L2 the served sidereal value at **75 arcsec** (a measured convention offset, not error — `FLG_TRUEPOS` makes served positions geometric), L3 the lunar node at **600 arcsec** (frame-dominated, with the margin *proved* against the wrong node model rather than asserted).
  - **NEVER re-record the JPL corpus to make a failure go away — that is the one thing it exists to prevent.** A red `test_independent_reference_corpus.py` is an accuracy defect measured against an outside authority, not a golden that drifted. Re-record **only** when a deliberate dependency or data bump explains the move, and say so in the PR. The same discipline the Swiss goldens get is not enough here: re-recording those loses a regression signal, re-recording this one destroys the only independent check in the repo.
    - **That rule is mechanical, not merely written down.** It used to be prose only, and `ci/fetch_reference_corpus.py` would happily overwrite the evidence — measured: a bare `python ci/fetch_reference_corpus.py` fetched Horizons and rewrote the committed file in place, no flag, no prompt, no diff. The recorder now refuses: overwriting an existing corpus needs `UPDATE_INDEPENDENT_CORPUS=1`, and recording is refused outright whenever `CI` is set **even with that flag** (a CI run must never write the evidence it is checking). `--check` is never gated — it writes nothing, so `python ci/fetch_reference_corpus.py --check` is the way to ask whether Horizons actually disagrees. The refusal sits *before* the network call, so a refused run costs no JPL round trip. `ci/tests/test_reference_corpus_fetcher.py` pins all of it.
    - The `CI` reading here is deliberately **stricter** than the Swiss goldens': `UPDATE_SWISS_GOLDENS` checks `os.environ.get("CI")` for *truthiness*, so an empty-string `CI` slips past it; the corpus recorder checks for *presence*. A false refusal costs one `unset CI`; a false permit costs the corpus. Do not "simplify" the two into agreement in the permissive direction.
    - The corpus polices its own honesty: `test_corpus_is_well_formed_and_declares_its_provenance` fails if either externally-sourced field is ever re-sourced from the engine, or stops declaring `independent_of_this_engine: true`. Do not "fix" that test by relabelling a field.
    - **One field is deliberately not independent** and must keep admitting it: `ayanamsa_deg` (`independent_of_this_engine: false`) — no solar-system ephemeris publishes a sidereal origin, so Horizons has no counterpart for it. Its *convention* is anchored separately by `tests/test_ayanamsa_zero_point.py` against the Calendar Reform Committee's published definition (Chitra/Spica at 180 deg sidereal; the decreed 23 deg 15' at the 1956 reform epoch), which rejects every other ayanamsa family but is too coarse to separate Lahiri from true-Chitra (~61 arcsec) — the 1e-6 deg corpus pin is what does that. Keep both; neither is re-recordable into agreement.
  - **The accuracy modules assert nothing in the fast `test` job, and that redundancy is load-bearing.** `tests/test_swiss_ephemeris.py`, `tests/test_independent_reference_corpus.py` and `tests/test_ayanamsa_zero_point.py` are gated on the `swiss_ephemeris` fixture, so with no data files they **skip green** — the `swiss-ephemeris` job is the only one that asserts them. Deleting that job, disabling it, **or merely narrowing its pytest target** leaves the whole workflow green with the accuracy gate switched off. `ci/tests/test_swiss_ephemeris_job.py::test_swiss_job_reaches_every_swiss_gated_module` closes the narrowing case and derives the protected set from the fixture itself (via `ast` over `tests/**/*.py`, so a module in a future subdirectory joins the set the day it lands, not the day someone remembers), and a directory target reaches modules at any depth beneath it — never add an accuracy module behind a target that job does not collect.
    - **The run line is not the only way to narrow a pytest run.** Three vectors reach the same outcome without editing it: `PYTEST_ADDOPTS` in the workflow's, job's or step's `env:` (GitHub Actions resolves all three into the step's environment); `addopts` in `pyproject.toml`; and `--collect-only`/`--co`, which deselects *nothing* — every gated module is still collected, so every reachability check truthfully answers "yes, it reaches them" — while not one test body executes and the job exits 0. `PYTEST_ADDOPTS` in scope for the accuracy job is refused outright (presence, not "is this particular value narrowing" — deciding that means reimplementing pytest's argument parser), and `--collect-only` is treated as narrowing.
    - **`--collect-only` defeats the fail-closed flag itself.** Measured: with `PYTEST_ADDOPTS='--collect-only'`, `pytest tests/ -q` under `REQUIRE_SWISS_EPHEMERIS=1` collects **912 tests, executes none, and exits 0** — fixtures do not run during collection, so the flag never evaluates and the accuracy gate reports success having asserted nothing. Never reason about that flag as if collection implied execution.
    - **One of those vectors cannot be guarded from inside pytest, and is not.** A narrowing `addopts` in `pyproject.toml` applies to *every* pytest invocation here, including `ci/tests/` — so it disarms the guard that would catch it. Measured, appending three lines to `pyproject.toml` and running `pytest ci/tests/ -q`: `addopts = "-k not_corpus"` → exit 5 (pytest itself fails, no tests ran); `-k 'not corpus'` and `-m 'not nothing'` → exit 1 (the pytest guard reds); but **`-k 'not narrow'` → exit 0** with the guard deselected, and **`--collect-only` → exit 0, 114 collected, 0 executed**. The last two are fail-open to any test-resident check. `ci/check_pytest_collection.py` closes them as a **plain `python` step** in `test.yml` (measured against the same `--collect-only` mutation: `pytest ci/tests/` exit 0, the checker exit 1). Keep it out of pytest — running it under pytest reintroduces exactly the hole it exists to close. It carries `--self-test` for the same reason the ephemeris fetcher does, and `test_a_workflow_step_runs_the_standalone_collection_check` fails if no workflow invokes it.
    - **The same argument applies to `PYTEST_ADDOPTS`, and round 1 of PR #66 got it wrong** — it moved the pyproject arm out of pytest and left the `PYTEST_ADDOPTS` detector *as a pytest test*, which the variable disables along with everything else. Measured: `PYTEST_ADDOPTS='--collect-only' pytest ci/tests/ -q` → exit 0, 119 collected, 0 executed, with `test_swiss_job_does_not_inject_pytest_addopts` among them. The checker therefore carries **two** further arms, both outside pytest: it refuses a **narrowing** `PYTEST_ADDOPTS` in **its own runtime environment** (catching workflow- and job-level `env:` injections, which land in its process too), and refuses `PYTEST_ADDOPTS` **declared anywhere in the workflow files** at any of the three `env:` levels or inline in a `run:` body (catching a step-level `env:` on a *sibling* pytest step, which the runtime read cannot see). Runtime is narrowing-only so a contributor's `PYTEST_ADDOPTS=-q` does not red the gate; declarations are presence-only because a workflow has no legitimate reason to carry one. `--self-test` refuses first under an injected environment — it must not certify a detector from inside an already-narrowed one.
    - **A `run:` body naming `PYTEST_ADDOPTS` at all is refused, and that bluntness is the point.** Round 2 analysed argv *position* — a leading `VAR=` prefix, or an `export` argument. Both are shell-**local**: every `run:` is a fresh shell, so neither escapes its own body. The form that actually reaches a later step is GitHub's own cross-step idiom, `echo "PYTEST_ADDOPTS=--collect-only" >> "$GITHUB_ENV"`, where the assignment is an *argument to `echo`* — so the position check said no. Measured with that one line appended to the swiss job's install step: the checker exited **0 "OK"**, all **149** guard tests passed, and the accuracy step would then have run under `--collect-only`. Telling a harmless echo from a poisoning one means modelling redirection, heredocs, `printf`, `tee`, indirection — and modelling is what failed. If a run body ever genuinely needs to name the variable, record the exception in `ci/check_pytest_collection.py` deliberately; do not relax the rule.
    - **Known bound:** the scan covers `.github/workflows`. A composite/reusable action pulled in with `uses:` can write `$GITHUB_ENV` from a file outside this repo, which nothing here reads. Documented in the checker, not silently assumed away.
  - `REQUIRE_SWISS_EPHEMERIS=1` turns "Swiss data is not active" from a skip into a hard failure. The detector is the `FLG_SWIEPH`/`FLG_MOSEPH` bit in `swe.calc_ut`'s retflag, because a Swiss request with the files absent still succeeds and returns plausible numbers (measured: retflag 65604, `FLG_SWIEPH` clear, `FLG_MOSEPH` set). Never soften that flag, and never let the fetcher warn-and-continue: a green accuracy job that actually ran on Moshier is precisely the defect this closed.
  - The data source is pinned to an upstream **commit**, not a branch, and each file to a sha256 (trust-on-first-use, like a lockfile). `ci/fetch_swiss_ephemeris.py` fails closed on a download error, a checksum mismatch, an unpinned (`null`) checksum, or a `base_url` that no longer contains `upstream_commit`; `--self-test` proves the verifier still rejects each of those. `EPHEMERIS_LICENSE.md` is a restatement of that manifest for a human reader, never a second source of truth: `ci/tests/test_ephemeris_license_doc.py` fails if it stops naming the manifest's **current** `upstream_repo` and `upstream_commit` (so a pin bump must update the doc in the same PR), if it names a URL recorded in `known_dead_sources` anywhere but its "Dead links" section, or if it generalises the probe beyond what was measured. Three astro.com *ephemeris-data* paths were probed on 2026-07-25 and all three 404 — that is the whole measurement, recorded as data in `ci/swiss_ephemeris.json` (`known_dead_sources`), and it says nothing about astro.com's licensing pages, which were not probed.
  - A pinned checksum or golden value moves only with a deliberate dependency/data bump, re-recorded in the same review (`UPDATE_SWISS_GOLDENS=1 python -m pytest tests/test_swiss_ephemeris.py`, refused when `CI` is set). `ci/tests/test_swiss_ephemeris_job.py` parses `test.yml` and fails if the job, its fetch step, its `--self-test` step, its frozen install, its manifest-keyed cache, or the env var goes missing — or if its pytest target stops collecting a `swiss_ephemeris`-gated module.
  - Locally: `python ci/fetch_swiss_ephemeris.py` once, then the Swiss tests stop skipping. Without the data they skip — that is the only skip in this suite, and the CI job above is what makes that skip impossible in CI. The **Docker** loop (`scripts/test-in-docker.*`) needs nothing fetched by hand: `Dockerfile.test` bakes the data in and sets `REQUIRE_SWISS_EPHEMERIS=1`, so a skip there means the bake broke.
- **The Swiss data is BAKED INTO the images — never volume-mounted.** Both `Dockerfile` and `Dockerfile.test` carry an `ephemeris` build stage running the same checksum-pinned `ci/fetch_swiss_ephemeris.py`, then `COPY --from=ephemeris` into `/app/data/ephe`. Do **not** reintroduce a runtime mount: a bind-mount over that path SHADOWS the baked files, and Docker silently creates an empty directory when the host path is absent. What holds this closed:
  - The `Dockerfile` used to `mkdir -p data/ephe` while a comment claimed the data "must be volume-mounted"; nothing ever mounted it, in any environment. Measured 2026-07-31 inside the image digest a deployed revision was actually serving: `data/ephe` held **0 files**, no `*.se1` existed anywhere on the filesystem, and `probe_ephemeris_source()` returned **retflag 65604** with `FLG_MOSEPH` set. Baking makes the data immutable with the image digest, so a rollback to an image is a rollback to its data. Cost, measured A/B on the same builder: **+1,924,448 bytes (1.84 MiB, +0.98%)**.
  - The build **fails closed**: the final stage runs `probe_ephemeris_source()` for **Sun and Moon** and exits non-zero unless both report `FLG_SWIEPH`. That `RUN` sits **after `USER appuser`** on purpose. An earlier draft ran it as root, passed, and still produced an image that computed on Moshier — the fetcher's `tempfile.mkstemp` left the files `0600`, and root could read what the service user could not. **A control verified as a different principal than the one that serves requests is not verified.** `ci/fetch_swiss_ephemeris.py` now `chmod`s `0644` before its atomic rename; keep both halves.
  - `app/ephemeris_guard.py` **refuses to start** outside `development`/`local`/`test` when either body is on the fallback, mirroring `app/auth.py`'s token guard. Both read their environment policy from `app/deployment.py` — one definition, two readers; never fork that list.
  - Sun-only checks are never enough: `sepl_18.se1` carries the planets and `semo_18.se1` the Moon, so a Sun-only probe passes on a data set that leaves every nakshatra, pada and dasha on the fallback.
  - `/healthz` reports `status: "degraded"` (still HTTP 200) when the fallback is in use — it used to answer a constant `"ok"`. `/source` additionally carries `ephe_loaded` + `ephemeris_source`, because a consumer that must fail a deploy on this signal needs an endpoint that is actually reachable: Cloud Run's frontend answers `/healthz` itself and the request never reaches the container.
  - `ci/tests/test_image_bakes_ephemeris.py` parses both Dockerfiles structurally (comments, commented-out instructions, orphan stages and shell-comment "assertions" all rejected), and `test.yml`'s `production-image-ephemeris` job builds the **production** image, re-asserts the retflag inside it as `appuser`, and proves the service dies when the data is shadowed.
- **Run the suite in BOTH ephemeris runtimes before pushing — CI runs both, and green in one proves nothing about the other.** Once you have fetched `data/ephe`, plain `pytest tests/` only ever exercises the *Swiss* runtime, while the `test` jobs in `test.yml` and `publish.yml` run with **no** data files. Measured 2026-07-31 (the bake PR): a `/healthz` change passed 652 local tests and failed CI three times, because `tests/test_endpoints.py` and `tests/test_docs_disabled.py` asserted a hardcoded `status == "ok"` that is only true when the Swiss data is present. Assertions outside `tests/test_swiss_ephemeris.py` must hold under **both** runtimes — assert the RELATIONSHIP (`status` follows `ephe_loaded`), never a constant.
  ```
  mv data/ephe data/_ephe_hidden && pytest tests/ -q ; mv data/_ephe_hidden data/ephe   # Moshier — what CI's `test` jobs run
  REQUIRE_SWISS_EPHEMERIS=1 pytest tests/ -q                                            # Swiss — what the accuracy job runs
  ```
- **Run the EXACT command CI runs, not an equivalent — `pytest X` and `python -m pytest X` are not the same command.** `python -m pytest` prepends the CWD to `sys.path`; the bare `pytest` console script does not. `ci/` has no `__init__.py` by design, so `from ci import …` resolves under the first form and raises `ModuleNotFoundError: No module named 'ci'` under the second. Measured 2026-07-31 (the bake PR): a `ci/tests/` test passed locally under `python -m pytest` and failed BOTH CI jobs, which run `pytest ci/tests/ -q` (`test.yml`, `publish.yml`). Load repo scripts by path via `importlib` in `ci/tests/`, never by package import. Every CI invocation now carries a `uv run --frozen` prefix (see **One resolve** above), which preserves that distinction exactly: `uv run --frozen pytest …` runs the console script and does **not** put the CWD on `sys.path`, just like the bare `pytest` it replaced, while `uv run --frozen python -m pytest …` does. The CI-invoked forms are, verbatim: `uv run --frozen pytest tests/ -q`, `uv run --frozen pytest ci/tests/ -q`, `uv run --frozen pytest tests/ --cov --cov-report=term-missing`, and — only for the accuracy job — `uv run --frozen python -m pytest tests/ -q`.
- Before every push, run the repo gate locally **from a lock-synced environment** (`uv sync --frozen --extra dev`, which takes its interpreter from `.python-version`): `python ci/check_no_proprietary_refs.py && uv run --frozen pytest tests/ -q && uv run --frozen pytest ci/tests/ -q`. The gate's own regression suite lives at `ci/tests/test_check_no_proprietary_refs.py` (outside the `tests/` tree, alongside the script it covers) — it is not swept by `pytest tests/ -q`, so both `test.yml` and `publish.yml` run it as a separate, explicit `uv run --frozen pytest ci/tests/ -q` step in CI.

## PR review & merge gate

Every PR here is reviewed **and** merged by a separate, independent review session, never the session that authored the change and never a human. Which model runs a review is the parent workspace's own policy, recorded there and deliberately not restated here — a copy in this file would drift silently against it. Its independent `--approve` authorizes the merge (`gh pr merge <n> --rebase --delete-branch`); the authoring session opens the PR and stops. Fail closed: no independent approval, no merge. The reviewer is bound by the product-neutrality rule above — its PR review text, like every artifact here, names no downstream consumer. Full hand-off mechanism and reviewer instructions live in the workspace-root `CLAUDE.md` → "PR review — separate independent review session".

## Active remediation

Audit findings against this repo are tracked in FORENSIC_AUDIT_2026-07-02.md (workspace repo, PR #1); its section 5 remediation batches are the work queue. Repo-relevant confirmed findings: MED-47 (Dockerfile ignores `uv.lock` and there is zero CVE scanning) and MED-74 (the proprietary-reference gate guarded only a superseded token and scanned only the latest commit — fixed: `_FORBIDDEN` is now a list seeded from `PROPRIETARY_REF_TOKENS` and the commit scan covers the pushed range; the owner still needs to provision the `PROPRIETARY_REF_TOKENS` GitHub Actions secret with the current brand token(s) for the extra pattern to become active). Each fix follows the parent workspace's test-first non-negotiable and the neutrality rule above; the preventive rules in this file are the standing encoding, not a restatement of the findings list.
